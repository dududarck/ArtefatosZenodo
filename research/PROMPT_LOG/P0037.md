# P0037

- **Timestamp (recebimento):** 2026-09-04T19:26:48Z
- **Fase Versus:** Fase 6 (Tests), Iteração 1
- **Contexto:** reteste manual do VA-human do UC-3 após a mudança A
  (observabilidade, P0035) — **novo comportamento**: loading infinito,
  **sem** mensagem de erro.

## Prompt literal do VA-human

```
Reteste manual da Fase 6 — UC-3:

FAIL real.

Comportamento observado:
- informo CPF válido;
- toco em "Carregar formulário";
- a UI entra em estado de carregamento;
- o formulário nunca aparece;
- nenhuma mensagem de erro aparece;
- o loading fica indefinidamente.

Portanto, o problema atual não está chegando ao ramo de erro observável.

Mantenha UC-2/UC-3 sem PASS e a Fase 6 aberta.

Agora diagnostique especificamente o request pendente:

1. instrumente EnrollmentViewModel.loadForm() com eventos de início/fim:
   loadForm_started; fetchForm_started; fetchForm_completed_success;
   fetchForm_completed_failure; loadForm_finished
2. registre timestamps/duração, sem CPF, URL completa ou PII;
3. confirme se fetchForm() retorna ou permanece suspenso;
4. confirme se URLSession: cria a task; inicia a task; recebe resposta
   HTTP; recebe bytes; completa; ou fica pendente;
5. registre URLSessionTaskMetrics de forma sanitizada: host; protocolo;
   duração; connect start/end; request start/end; response start/end;
6. confirme se o timeout de 30s está realmente aplicado à
   URLRequest/URLSessionTask;
7. confirme os valores efetivos de: timeoutInterval da request;
   timeoutIntervalForRequest; timeoutIntervalForResource;
8. após 35–40s sem retorno, registre explicitamente que o timeout
   esperado não disparou;
9. verifique: continuation nunca retomada; delegate segurando
   completionHandler; stream/body não finalizado; retry implícito;
   task recriada; await preso em outra operação;
10. não aumente timeout ainda;
11. não altere FormularioInscricao ainda;
12. não faça nova correção por hipótese.

Apresente: em qual await/tarefa o fluxo fica preso; duração observada;
se o timeout realmente dispara; causa objetiva; correção mínima
proposta. Só depois proponha B/C/D ou outra correção.
```

## Interpretação

3º achado adversarial sobre o mesmo fluxo (UC-3), agora com sintoma
NOVO e mais grave: nem sucesso nem erro — a UI trava em loading
permanente. Isso é incompatível com a hipótese de `.transport` (timeout
de 30s do `URLRequest.timeoutInterval`, que sempre resolve a
`await`/completion em algum momento finito) ser a única explicação —
exige investigar se a `Task`/`await` está genuinamente presa (nunca
retorna) em vez de apenas demorar. Instrumentação obrigatória antes de
qualquer nova correção; nenhuma alteração de timeout/modelo/lifecycle
ainda.
