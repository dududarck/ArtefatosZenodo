# P0045

- **Timestamp (recebimento):** 2026-09-05 (system date; hora exata não capturada no início — corrigir em entradas futuras)
- **Fase IACDM/Versus no momento:** Fase 6 (Testes), iteração 1
- **Origem:** VA-human (chat), com contexto de IDE apontando para `aplicativo ios/GoogleService-Info.plist`
- **Safeguards no momento:** S0–S5 ok, S7 ok, **S6 warning** (0/3 falhas consecutivas não diagnosticadas — apenas o aviso padrão de "implementação ativa")

## Prompt preservado (verbatim)

> Atualização da Fase 6 — Firebase/Notificações.
>
> Recebi agora o arquivo institucional real: **GoogleService-Info.plist**.
>
> Validação preliminar humana: o arquivo é do app iOS; BUNDLE_ID = `br.jus.tjac.esjud.app`; FCM está habilitado; portanto **EXT-DEP-01 NÃO é mais um bloqueio externo ativo**.
>
> Situação do BLOCO C: UC-7 AVA/Moodle: PASS manual. UC-8 Notícias: PASS manual. UC-9 Notificações: ainda **NÃO PASS**, pois o caminho Firebase real precisa agora ser integrado e validado.
>
> **Faça a integração real do GoogleService-Info.plist no app iOS e feche o fluxo FCM de ponta a ponta.**
>
> IMPORTANTE: use o arquivo real recebido; não exponha API keys, sender IDs, app IDs ou outros valores sensíveis em logs/relatórios; não fabrique plist; não altere bundle identifier; não marque push como PASS apenas por compilar.
>
> Investigue e implemente obrigatoriamente:
> 1. **INCLUSÃO DO PLIST** — adicionar ao target `ESJUDApp`, Target Membership, Copy Bundle Resources; `Bundle.main` deve encontrá-lo em runtime.
> 2. **FIREBASE INIT** — revisar init defensiva; `FirebaseApp.configure()` deve rodar normalmente com config válida; degradação graciosa só como proteção.
> 3. **FIREBASE MESSAGING** — confirmar linkagem; `Messaging.delegate`; token FCM real; evidência sanitizada apenas, nunca o token completo.
> 4. **APNS** — solicitar autorização; registrar para remote notifications; `didRegisterForRemoteNotificationsWithDeviceToken`; passar o token APNs para o `Messaging`; verificar `didFailToRegister`.
> 5. **ENTITLEMENTS/CAPABILITIES** — Push Notifications; Background Modes > Remote notifications; `aps-environment`; não inventar entitlement se a assinatura não suportar.
> 6. **BACKEND** — como o Android envia/registra o token FCM; reproduzir o contrato existente; sem novo backend.
> 7. **RECEBIMENTO FOREGROUND** — `UNUserNotificationCenterDelegate`; sem duplicação.
> 8. **RECEBIMENTO BACKGROUND/TERMINATED** — dispositivo físico se necessário; distinguir limitações do Simulador de falhas reais; navegação/deep-link ao tocar.
> 9. **TELA DE NOTIFICAÇÕES/POLLING** — preservar o polling `/api/public/feed`; FCM não substitui/quebra a lista; badge/marcar todas como lidas; sem duplicação entre push e feed.
> 10. **EXT-DEP-01** — atualizar o registro: antes blocked, agora recebido em 05/09/2026, status desbloqueado; não apagar histórico; registrar a transição de estado.
> 11. **TESTES** — preservar os existentes; adicionar para a lógica testável; teste unitário não é prova de push real; validação humana/runtime necessária.
> 12. **BUILD** — rodar testes; build do Package; build do App target; reportar resultados reais.
>
> Critério de saída do UC-9: polling continua funcional; Firebase inicializa com config real; app obtém token FCM/APNs quando o ambiente permitir; push real é recebido; notificação aparece corretamente; badge/feed permanecem coerentes; sem duplicação indevida; validação manual humana concluída.
>
> Se houver alguma etapa que obrigatoriamente exija dispositivo físico, conta Apple institucional, APNs key/certificate ou envio pelo Firebase Console/backend, diga exatamente qual ação humana preciso executar.
>
> Não avance para Fase 7.

## Ação atribuída

Integração real do `GoogleService-Info.plist` no target do app iOS; fechamento do caminho FCM ponta-a-ponta no `AppDelegate` (autorização de notificação, APNs, token FCM sanitizado, tap/deep-link, background/silent, registro de dispositivo no painel reproduzindo o contrato Android); entitlements + Background Modes; testes da lógica testável (parse de payload, evidência de token); builds reais; atualização do registro EXT-DEP-01. Sem avanço de fase.
