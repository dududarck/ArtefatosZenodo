# P0041

- **Timestamp (recebimento):** 2026-09-05T20:44:57Z
- **Fase Versus:** Fase 6 (Tests), Iteração 1
- **Contexto:** reteste manual do UC-3 após P0040 — **loading infinito
  RESOLVIDO**. Agora o fluxo chega a um resultado terminal, mas o
  backend retorna "Formulário não disponível (etapa: timeout). Verifique
  CPF, vagas e período de inscrição." e o formulário não é exibido.

## Prompt literal do VA-human

```
Novo reteste manual do UC-3:

O loading infinito foi resolvido.

Agora, ao tentar carregar o formulário, o formulário não aparece e o app
exibe diretamente:
"Formulário não disponível (etapa: timeout). Verifique CPF, vagas e
período de inscrição."

Portanto:
- a UI não fica mais presa;
- o fluxo chega a um resultado;
- porém o backend retorna indisponibilidade/timeout e o formulário não é exibido.

Não marque UC-3 como PASS ainda.

Faça validação comparativa:
1. confirme status HTTP/code desse caso;
2. reproduza o mesmo curso via curl;
3. teste pelo menos 2 outros cursos com inscrições abertas e vagas;
4. determine se: somente esse curso falha; vários cursos falham; todos falham;
5. se outro curso válido carregar formulário no app e este mesmo curso
   falhar também via curl, classifique como indisponibilidade do
   backend/curso, não defeito do iOS;
6. se nenhum curso carregar no app, continue investigando o cliente.

Não altere código antes dessa comparação.
```

## Interpretação

P0040 confirmado resolvido (loading infinito). Novo estado: a máquina de
estados agora funciona e mapeia HTTP 422 (`FORM_UNAVAILABLE`) para
`.indisponivel(<message do backend>)`, exibindo o texto do backend —
comportamento CORRETO do cliente. A questão agora é se o 422 "etapa:
timeout" é (a) indisponibilidade real do backend/curso (não é defeito
iOS) ou (b) sistêmico/todos os cursos (aí ainda há algo a investigar no
cliente). Validação comparativa por curl, sem alterar código.
