# P0010

- **Timestamp (UTC):** 2026-08-29T23:06:02Z
- **Ator:** VA-human.
- **Tipo de evento:** retomada da implementação da Fase 5 a partir do
  estado atual (7/23 módulos), com prioridade explícita: M-04, M-08,
  M-09, M-10, M-11, M-12 (domínio/infraestrutura restantes), seguidos
  da camada de apresentação somente após esses estarem verificados.
  Regras reforçadas: nenhuma decisão arquitetural nova sem aprovação
  explícita; parar para aprovação humana se surgir necessidade de
  alterar arquitetura/módulos/interfaces (decorrente do achado M-23,
  P0008/P0009); micro-check adversarial de divergência de
  implementação por módulo; nenhum módulo marcado concluído sem
  evidência real de compilação.

## Prompt (representação exata e reprodutível)

> Retome a Fase 5 a partir do estado atual do Versus/IACDM.
>
> Implemente os módulos restantes exatamente conforme a arquitetura
> V(2) aprovada, mantendo a ordem e a granularização já definidas.
>
> Prioridade imediata:
> - M-04 courses
> - M-08 moodle-web
> - M-09 news
> - M-10 admin-feed
> - M-11 push-transport
> - M-12 course-reminders
>
> Depois disso, avance para a camada de apresentação somente quando os
> módulos de domínio/infraestrutura pendentes estiverem implementados e
> verificados.
>
> Para cada módulo:
>
> 1. consulte previamente specs/ e a arquitetura V(2);
> 2. implemente somente o escopo daquele módulo;
> 3. não introduza novas decisões arquiteturais silenciosamente;
> 4. se surgir necessidade de alterar arquitetura, adicionar módulo,
>    fundir módulos ou mudar interfaces, PARE e peça aprovação
>    explícita do VA-human antes de implementar a mudança;
> 5. execute compilação/verificação real usando Xcode/xcodebuild;
> 6. registre falhas de compilação e correções;
> 7. execute o adversarial micro-check de implementation drift,
>    verificando explicitamente onde a implementação diverge de specs/;
> 8. registre o resultado no research log e nos artefatos persistentes;
> 9. não marque o módulo como concluído sem evidência objetiva de
>    compilação/verificação;
> 10. preserve a sequência e os timestamps reais.
>
> Regra adicional decorrente do achado M-23: qualquer alteração
> estrutural nova introduzida durante a Fase 5 deve receber aprovação
> explícita do VA-human antes da implementação.
>
> Prossiga conforme o estado atual do Versus e pare sempre que houver
> uma decisão humana obrigatória.
