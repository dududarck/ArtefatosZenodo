# P0003

- **Timestamp (UTC):** 2026-08-26T22:21:02Z
- **Ator:** VA-human, via mensagem de usuário direta ao GA.
- **Tipo de evento:** autorização explícita para iniciar formalmente a
  Fase 0 do IACDM — encerra o período de preparação pré-experimental
  coberto por P0001 e P0002.
- **Estado do Versus imediatamente antes desta ação** (via
  `mcp__versus-claude__get_phase_state`): `currentPhase = 0`,
  `currentIteration = 1`, `phase0Score = null`, `decisionsCount = 0`,
  `recentDecisions = []`, safeguards S0–S7 todos "ok",
  `updatedAt = "2026-08-26T22:20:37.850Z"`.

## Prompt (representação exata e reprodutível)

> start

## Ação do GA em resposta a P0003

1. Chamada a `mcp__versus-claude__get_phase_state()` e
   `mcp__versus-claude__get_phase_guidance()` (ferramentas MCP,
   evidência automática) para carregar o estado e as instruções
   completas da Fase 0 antes de qualquer ação.
2. Registro deste prompt como P0003 (este arquivo) e da entrada
   correspondente em `RESEARCH_LOG.md`, antes de iniciar o trabalho de
   Fase 0 propriamente dito, conforme obrigação estabelecida em
   `CLAUDE.md`.
3. A partir daqui, a condução da Fase 0 (perguntas ao usuário, síntese,
   pontuação de convergência) passa a ser conduzida pelas ferramentas
   MCP `mcp__versus-claude__*` como sistema de registro primário da
   metodologia; este `research/` continua registrando, em paralelo,
   decisões, achados e eventos relevantes como camada de reprodutibilidade
   científica independente do Versus.
