# P0035

- **Timestamp (recebimento):** 2026-08-31T22:44:45Z
- **Fase Versus:** Fase 6 (Tests), Iteração 1
- **Contexto:** aprovação parcial da correção do achado P0034 — só a mudança **A (observabilidade)**.

## Prompt literal do VA-human

```
Aprovo primeiro somente a mudança A — observabilidade.

Objetivo:
na próxima execução manual, não colapsar NetworkError em "Erro ao carregar formulário".

Quero que o EnrollmentViewModel/UI diferencie de forma segura:
- timeout/transport;
- decoding;
- httpStatus + message do backend;
- certificado indisponível;
- URL inválida.

Não exponha dados pessoais nem payload sensível na UI ou logs.

Não altere ainda:
- timeout de 30 s;
- modelo FormularioInscricao;
- lifecycle do EnrollmentViewModel;
- news.materia.

Depois:
1. compile;
2. mantenha os testes automatizados verdes;
3. peça novo reteste manual do mesmo curso/fluxo;
4. registre o erro real observado;
5. só então proponha a correção causal mínima.

Mantenha a Fase 6 aberta.
```

## Interpretação

Aplicar SÓ a mudança A do diagnóstico de P0034: tornar o modo de falha
de rede visível e diferenciado (transport/timeout, decoding, httpStatus
+ message do backend, certificado indisponível, URL inválida) no
`EnrollmentViewModel`/UI, sem vazar CPF/PII nem payload sensível — nem
na UI nem nos logs. Explicitamente NÃO mexer ainda em: timeout de 30 s,
modelo `FormularioInscricao`, ciclo de vida do `EnrollmentViewModel`,
`news.materia`. Depois: compilar, manter os 74 testes verdes, pedir
reteste manual do mesmo curso, registrar o erro real observado, e só
então propor a correção causal mínima. Fase 6 permanece aberta.
