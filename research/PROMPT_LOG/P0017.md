# P0017

**Timestamp:** 2026-08-31T03:26:18Z

**Prompt (verbatim):**

> Resultado real do smoke test inicial:
>
> O app não conseguiu iniciar porque ocorreu a exceção:
>
> `FirebaseApp.configure() could not find a valid GoogleService-Info.plist in your project.`
>
> Isso confirma empiricamente a pendência registrada em D0051.
>
> Decisão do VA-human:
>
> Como o GoogleService-Info.plist depende de acesso institucional externo ao projeto Firebase do TJAC e já foi registrado como dívida técnica/dependência externa, o Firebase NÃO deve impedir a execução das demais funcionalidades do aplicativo.
>
> Implemente tratamento de configuração opcional do Firebase no app target, com degradação graciosa:
>
> 1. antes de chamar FirebaseApp.configure(), verifique objetivamente se existe uma configuração Firebase válida no bundle;
> 2. se existir, inicialize Firebase normalmente e configure FirebaseMessaging;
> 3. se não existir:
>    - NÃO chame FirebaseApp.configure();
>    - NÃO cause crash;
>    - NÃO fabrique configuração;
>    - NÃO gere GoogleService-Info.plist;
>    - desabilite apenas a funcionalidade de push FCM;
>    - permita que o restante do aplicativo funcione normalmente;
> 4. registre por logging diagnóstico, sem dados sensíveis, que Firebase/FCM está indisponível por ausência de configuração;
> 5. preserve AdminFeed, polling e CourseReminders, que não devem depender do Firebase;
> 6. mantenha M-11 e a arquitetura já aprovada;
> 7. não trate essa alteração como evidência de push funcional;
> 8. registre no RESEARCH_LOG que o primeiro smoke test revelou um crash de inicialização causado pela ausência esperada do GoogleService-Info.plist e que foi necessária uma correção para graceful degradation;
> 9. compile novamente com xcodebuild;
> 10. se o build passar, peça ao VA-human para repetir o smoke test manual;
> 11. não marque o critério do smoke test como concluído até eu informar o resultado real.
>
> Se essa correção exigir mudança arquitetural não prevista, pare antes e peça aprovação.
>
> Prossiga pelo fluxo normal do Versus/IACDM.
