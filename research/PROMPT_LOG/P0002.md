# P0002

- **Timestamp (UTC):** 2026-08-26T22:06:12Z (início da coleta do
  manifesto de baseline, primeira ação concreta em resposta a este
  prompt).
- **Ator:** VA-human, via mensagem de usuário direta ao GA.
- **Tipo de evento:** instrução de preparação pré-experimental — Parte A
  (identificação reproduzível da baseline Android sem Git) e Parte B
  (preenchimento inicial de `specs/` a partir da baseline), ainda antes
  da Fase 0 do IACDM.
- **Estado do Versus no momento do prompt:** inalterado desde P0001 —
  `currentPhase = 0`, `currentIteration = 1`, nenhuma decisão registrada
  via MCP.

## Prompt (representação exata e reprodutível)

> Antes de iniciar a Fase 0 do IACDM, conclua a preparação
> pré-experimental da baseline e do repositório `specs/`.
>
> NÃO inicie ainda a Fase 0.
>
> ## Parte A — Identificação reproduzível da baseline Android
>
> Como a implementação Android de referência localizada em:
>
> `./ESJUDAC android referencia para criar ios/`
>
> não possui repositório Git, estabeleça uma identificação reproduzível
> da baseline SEM modificar qualquer arquivo do projeto Android e SEM
> criar um repositório Git dentro dele.
>
> Crie em `research/` um manifesto da baseline contendo:
>
> * timestamp da coleta;
> * caminho da baseline;
> * algoritmo utilizado;
> * hashes SHA-256 dos arquivos da implementação Android necessários
>   para identificar o estado observado;
> * hash agregado/determinístico do manifesto ou mecanismo equivalente
>   que permita verificar posteriormente se a baseline foi alterada.
>
> Regras:
>
> * Não copie conteúdos sensíveis para `research/`.
> * Não exponha segredos, senhas, chaves, certificados ou conteúdo de
>   arquivos `.p12`.
> * Se arquivos sensíveis fizerem parte da baseline, registre apenas
>   metadados seguros e/ou hash criptográfico, nunca seu conteúdo.
> * Não modifique a aplicação Android.
> * Registre esta ação no `RESEARCH_LOG.md` e demais registros
>   persistentes aplicáveis.
> * Atualize `BASELINE.md` para referenciar o manifesto produzido.
>
> ## Parte B — Preparação do repositório de conhecimento IACDM
>
> Após fixar a baseline, prepare o repositório de conhecimento
> pré-Fase-0 exigido pelo Versus/IACDM.
>
> Inspecione profundamente a implementação Android localizada em:
>
> `./ESJUDAC android referencia para criar ios/`
>
> e preencha a estrutura já existente em `./specs/` com MATERIAL INICIAL
> DE REFERÊNCIA apropriado para a implementação instalada do Versus.
>
> Não crie um sistema paralelo de especificações.
>
> ### Regras
>
> 1. NÃO modifique a aplicação Android de referência.
> 2. NÃO crie código Swift ou iOS de produção.
> 3. NÃO crie ainda a arquitetura iOS.
> 4. NÃO tome decisões de implementação pertencentes a fases
>    posteriores.
> 5. Trate o código Android como evidência sobre o sistema existente,
>    não como uma especificação infalível.
> 6. Diferencie claramente:
>
>    * fatos diretamente evidenciados;
>    * comportamentos inferidos;
>    * pressupostos;
>    * ambiguidades;
>    * perguntas que exigem confirmação do VA-human.
> 7. Preserve rastreabilidade para arquivos, classes, resources,
>    manifests, configurações, endpoints e demais evidências relevantes.
> 8. Identifique, quando sustentado por evidência:
>
>    * vocabulário do domínio;
>    * atores;
>    * funcionalidades;
>    * fluxos visíveis ao usuário;
>    * entidades;
>    * APIs;
>    * autenticação;
>    * persistência;
>    * integrações;
>    * dependências;
>    * comportamentos específicos do Android;
>    * comportamentos externamente observáveis;
>    * restrições.
> 9. Não transforme silenciosamente decisões de implementação Android em
>    requisitos obrigatórios do iOS.
> 10. Não invente regras institucionais que não possam ser estabelecidas
>     pelas evidências disponíveis.
> 11. Preserve questões não resolvidas para a Fase 0 em vez de adivinhar
>     respostas.
> 12. Atualize automaticamente todos os registros científicos
>     persistentes aplicáveis conforme as instruções já estabelecidas em
>     `CLAUDE.md`.
>
> Ao terminar, reporte:
>
> 1. como a baseline foi identificada e onde está o manifesto;
> 2. quais arquivos/diretórios de `specs/` foram preenchidos;
> 3. quais partes do Android foram inspecionadas;
> 4. principais fatos diretamente evidenciados;
> 5. principais inferências ou pressupostos;
> 6. ambiguidades ainda não resolvidas;
> 7. se o repositório está pronto para iniciar formalmente a Fase 0.
>
> Depois PARE.
>
> Não execute `start` e não inicie automaticamente a Fase 0.

## Ações executadas pelo GA em resposta a P0002 (resumo — ver `RESEARCH_LOG.md` para cronologia com timestamps)

1. Verificação de repositórios Git aninhados dentro da baseline —
   descoberta de que `api/`, `painel-web/` e `whatsapp-api/` são
   repositórios Git próprios (não detectado em P0001, que só verificou a
   raiz da baseline).
2. Enumeração determinística de 121 arquivos dentro do escopo
   `app/` + arquivos de configuração de raiz (excluindo caches
   `.gradle/`, `.idea/`, `.kotlin/`, `app/build/`), cálculo de SHA-256 e
   tamanho de cada um via `shasum -a 256`/`stat`, e cálculo de um hash
   agregado SHA-256 sobre a tabela ordenada de caminho+tamanho+hash.
3. Identificação de 5 arquivos sensíveis dentro do escopo (dois
   certificados `.p12`, um keystore `.jks`, `google-services.json`,
   `local.properties`) — apenas hash e metadados registrados, nenhum
   conteúdo lido ou reproduzido.
4. Criação de `research/BASELINE_MANIFEST.md` e atualização de
   `research/BASELINE.md` com referência a ele.
5. Leitura extensiva (não exaustiva) do código Kotlin do módulo `app/`:
   ponto de entrada (`EsjudacApp`, `MainActivity`), navegação, todos os
   modelos de domínio, todas as interfaces Retrofit e seus modelos de
   API, todos os repositórios, DataStores locais, serviço/worker/helper
   de notificações, `WebViewScraper`, uma tela completa (`LoginScreen`)
   e trechos (`grep`) de outras telas, arquivos de configuração XML
   (`AndroidManifest.xml`, `network_security_config.xml`,
   `file_provider_paths.xml`), `strings.xml`, `libs.versions.toml`.
6. Identificação de uma chave de API embutida em texto puro no
   código-fonte Kotlin (`NetworkModule.PAINEL_API_KEY`) — tratada como
   achado de segurança, valor **não** reproduzido em nenhum documento de
   `specs/` ou `research/`.
7. Criação de 8 documentos em `specs/` (`domain/`, `technical/`,
   `models/`, `references/`, `examples/`), todos distinguindo
   explicitamente [FATO] / [INFERÊNCIA] / [PRESSUPOSTO] / [AMBIGUIDADE]
   / [PERGUNTA-VA], com rastreabilidade a arquivos-fonte específicos.
8. Nenhuma chamada a ferramentas MCP `mcp__versus-claude__*` que alteram
   estado. A Fase 0 do IACDM permanece **não iniciada**.
