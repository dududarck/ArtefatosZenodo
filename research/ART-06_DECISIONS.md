# Registro de Decisões

Registra decisões tomadas no âmbito do estudo/projeto: aceitas,
rejeitadas ou adiadas, com autor da decisão (GA propõe, VA-human decide,
salvo indicação contrária) e justificativa. Decisões formais da
metodologia IACDM (registradas via `mcp__versus-claude__record_decision`)
são espelhadas aqui para consolidação do registro científico, mas o
estado autoritativo da metodologia em si permanece em `.versus/`.

Nenhuma decisão é removida ou reescrita; alterações de rumo geram uma
nova entrada que referencia a decisão anterior.

| ID | Timestamp (UTC) | Decisão | Status (aceita/rejeitada/adiada) | Autor | Justificativa | Referência (prompt/gate) |
|---|---|---|---|---|---|---|
| D0001 | 2026-08-26T21:52:28Z | Criar instrumentação de pesquisa (`research/`) antes de iniciar a Fase 0 do IACDM, sem alterar `.versus/`, `.claude/`, `.mcp.json` ou `specs/` | Aceita | VA-human (instrução direta do operador/pesquisador) | Necessidade de um registro científico e de reprodutibilidade independente do estado interno do Versus, estabelecido antes de qualquer atividade metodológica de desenvolvimento | P0001 |
| D0002 | 2026-08-26T21:5xZ | Usar o utilitário de shell (Bash/heredoc) em vez das ferramentas dedicadas de escrita de arquivo para criar os arquivos em `research/`, pois o hook `phase-gate.js` do Versus bloqueia gravações fora de uma lista fixa de caminhos (`specs/`, `.versus/`, `.claude/`, etc.) durante as Fases 0–4 | Aceita (decisão técnica do GA, não decisão metodológica do IACDM) | GA | O bloqueio existe para impedir código de produção prematuro; os arquivos de pesquisa não são código de produção, não são artefatos do Versus e não alteram estado de conformidade — usar Bash é o caminho tecnicamente disponível, não uma tentativa de contornar o propósito do safeguard | P0001 |

## Decisões pendentes/adiadas para as fases metodológicas

Nenhuma decisão de arquitetura, escopo ou tecnologia do produto iOS foi
tomada nesta etapa. Tais decisões são explicitamente adiadas para as
fases correspondentes do IACDM (Fase 0 em diante), conforme instrução
do usuário de não iniciar ainda a Fase 0.
| D0003 | 2026-08-26T22:06:12Z | Identificar a baseline Android via manifesto de hashes SHA-256 (arquivo por arquivo + hash agregado), escopado a `app/` + arquivos de configuração de raiz do Gradle, excluindo caches (`.gradle/`, `.idea/`, `.kotlin/`, `app/build/`) | Aceita | GA (técnica), instruída pelo VA-human (P0002, Parte A) | Sem Git na baseline, hash de arquivo é o mecanismo mais direto e verificável de identificação reproduzível; caches são regenerados automaticamente e não são estáveis entre execuções, por isso não fazem parte da identidade da baseline | P0002 |
| D0004 | 2026-08-26T22:06:12Z | Tratar os subprojetos `api/`, `painel-web/`, `whatsapp-api/` (cada um um repositório Git próprio) como identificados por commit HEAD, e não incluí-los no hash agregado de arquivos da baseline Android | Aceita | GA (técnica) | Cada um já possui um identificador de estado nativo e reproduzível (commit SHA); hashear centenas de arquivos de `node_modules` seria impraticável e não agregaria valor de identificação, já que dependências não fazem parte do código do produto | P0002 |
| D0005 | 2026-08-26T22:06:12Z | Popular `specs/domain`, `specs/technical`, `specs/models`, `specs/references` e `specs/examples` com material derivado da leitura do código Android, distinguindo fato/inferência/pressuposto/ambiguidade/pergunta em cada afirmação, sem tocar `specs/competitors`, `specs/design`, `specs/datasets`, `specs/validation` (fora do escopo do que há evidência para preencher agora) | Aceita | GA (técnica), instruída pelo VA-human (P0002, Parte B) | Regra 5 do prompt exige tratar o Android como evidência, não especificação; pastas sem evidência disponível (concorrentes, design, datasets, validação) não devem ser preenchidas com conteúdo fabricado | P0002 |
| D0006 | 2026-08-26T22:1xZ | Não reproduzir o valor da chave de API (`PAINEL_API_KEY`) encontrada em texto puro no código-fonte Kotlin em nenhum documento de `specs/` ou `research/`, apenas registrar sua existência, localização e o achado de segurança | Aceita | GA (técnica) | Regra explícita do usuário de nunca expor segredos em `research/`; o mesmo princípio foi estendido a `specs/` por prudência, mesmo não sendo um arquivo de certificado/senha classicamente sensível | P0002 |
| D0007 | 2026-08-26T22:23:47Z | DELIVERY TARGET (Fase 0, Passo 0.5): Produto completo — todas as funcionalidades do Android ESJUDAC entregues em um único ciclo no iOS | Aceita | VA-human (via AskUserQuestion no fluxo IACDM) | Registrada formalmente também no Versus via `record_decision` (id `0d869f44-3822-417e-9036-fd660870f689`, phase=0, category=scope) | P0003 |
| D0008 | 2026-08-26T22:25:13Z | Nome institucional por extenso confirmado: "Escola do Poder Judiciário do Acre" (marca curta "ESJUD" mantida) | Aceita | VA-human | Resolve a Pergunta 1 de `specs/references/perguntas-para-va-human.md`; registrado no Versus (category=requirement, id `21450e5e-...`) | P0003 |
| D0009 | 2026-08-26T22:25:15Z | "Cronograma de cursos/eventos" confirmado como FORA DE ESCOPO do produto iOS completo | Aceita | VA-human | Resolve a Pergunta 2 de `specs/references/perguntas-para-va-human.md`; código Android atual não implementa mais essa funcionalidade; registrado no Versus (category=scope, id `341caf24-...`) | P0003 |
| D0010 | 2026-08-26T22:35:32Z | Plataforma-alvo: iOS 17+ | Aceita | VA-human | Nível 2 (Where); registrado no Versus (constraint, id `7e667f77-...`) | P0003 |
| D0011 | 2026-08-26T22:35:33Z | Sem prazo institucional rígido; ritmo da metodologia IACDM | Aceita | VA-human | Nível 2 (When); registrado no Versus (constraint, id `93f6b57b-...`) | P0003 |
| D0012 | 2026-08-26T22:35:35Z | Backends existentes (Moodle, ESJUD, Certidão, Certificados Externos, Painel) tratados como contratos fixos e imutáveis — nenhuma mudança de backend no escopo | Aceita | VA-human | Nível 2 (How); registrado no Versus (constraint, id `d8e81b7d-...`) | P0003 |
| D0013 | 2026-08-26T22:35:37Z | Armazenamento de credenciais no iOS via Keychain, divergindo deliberadamente do comportamento em texto puro observado no Android | Aceita | VA-human | Resolve Pergunta 4 de `specs/references/perguntas-para-va-human.md`; registrado no Versus (architecture, id `b9382b1a-...`) | P0003 |
| D0014 | 2026-08-26T22:42:37Z | Certificado mTLS embutido no bundle do app iOS (mesma abordagem do Android) | Aceita | VA-human | Resolve parte da Pergunta 5; registrado no Versus (architecture, id `ff207ea1-...`) | P0003 |
| D0015 | 2026-08-26T22:42:39Z | Chave de API do Painel tratada como segredo de baixo risco, mantida embutida no cliente iOS | Aceita | VA-human | Resolve Pergunta 3; registrado no Versus (constraint, id `8879ae8d-...`) | P0003 |
| D0016 | 2026-08-26T22:42:40Z | Sem expiração local de sessão — replica comportamento do Android | Aceita | VA-human | Resolve Pergunta 6; registrado no Versus (requirement, id `7fa7d4fd-...`) | P0003 |
| D0017 | 2026-08-26T22:42:41Z | Logout limpa TODOS os dados locais (sessão, cursos, notificações) — diverge do Android | Aceita | VA-human | Resolve Pergunta 7; registrado no Versus (requirement, id `a3b04a36-...`) | P0003 |
| D0018 | 2026-08-26T22:43:53Z | Tech Feasibility (Nível 4): paridade Android→iOS verificada para 6 mecanismos de plataforma, sem blocker; push via Firebase SDK iOS (backend fixo); lembretes recomendados via notificações locais agendadas por data em vez de worker periódico | Aceita | GA (análise técnica), sem objeção do VA-human | Registrado no Versus (technology, id `b6817156-...`); decisão de arquitetura definitiva fica para a Fase 1 | P0003 |
| D0019 | 2026-08-26T22:51:06Z | Production Capacity Check (Nível 5): único gap = design visual iOS; resolvido como design assistido por IA seguindo Apple HIG, usando telas Android como referência funcional (não espelhamento visual) | Aceita | VA-human | Registrado no Versus (process, id `b4c0c5dc-...`) | P0003 |
| D0020 | 2026-08-26T22:5xZ | Correção humana (não uma decisão de produto): Teach-Back da Iteração 1 da Fase 0 REJEITADO como "Correto" — classificado pelo VA-human como "Quase — pequenos ajustes" — por vazamento de decisões de solução (Keychain, MVVM, Apple HIG, Firebase SDK, notificações locais, bundling do .p12) para dentro da síntese de compreensão do problema, e por inconsistência entre "ambiguidade não bloqueante" e "critério de sucesso" quanto à compatibilidade push iOS↔painel-web | Registrada como achado — não é uma decisão aceita/rejeitada/adiada de produto, é uma correção de processo | VA-human | Denominação neutra adotada por instrução do VA-human: "possível vazamento de decisões de solução para a Fase 0 (phase-boundary leakage)". Não classificado como bug do Versus. Decisões D0007–D0019 permanecem válidas como respostas humanas reais; o que muda é como a próxima síntese de Teach-Back deve apresentá-las (distinguindo confirmado vs. provisório). Ver `RESEARCH_LOG.md`, entrada P0004, para detalhamento completo | P0004 |
| D0021 | 2026-08-26T23:27:32Z | Compatibilidade push iOS↔painel-web RECLASSIFICADA de "ambiguidade não bloqueante presumida" para FATO VERIFICADO por leitura de código: `painel-web/src/lib/devicesRepo.js` já modela `platform: "ios"`; `painel-web/src/lib/push.js` usa Firebase Admin SDK genérico com fallback garantido para polling do feed. Gap menor não bloqueante: ausência de bloco `apns` explícito no payload — anotado para Fase 1 | Aceita (correção de classificação, não decisão de produto) | GA (investigação factual solicitada implicitamente pela correção do VA-human em P0004) | Resolve diretamente o achado secundário de P0004 (inconsistência entre "não bloqueante" e "critério de sucesso"); registrado no Versus (requirement, id `7428711c-...`) | P0004 |
| D0022 | 2026-08-26T22:5xZ | Decomposição inicial de 11 módulos apresentada pelo GA (Fase 1) NÃO aprovada como está — VA-human solicitou ajuste de granularidade (M-10 notifications e M-11 app-ui excessivamente amplos; M-01/M-02 precisam de interfaces mais explícitas) | Correção solicitada (não é rejeição de escopo, é ajuste de granularidade arquitetural) | VA-human | Ver PROMPT_LOG/P0005.md para o texto completo da correção | P0005 |
| D0023 | 2026-08-29T16:31:34Z | Decomposição REVISADA aprovada: 22 módulos (12 domínio/infraestrutura + 10 apresentação por funcionalidade); M-10 notifications dividido em admin-feed/push-transport/course-reminders; M-11 app-ui dividido em app-shell + 9 módulos de apresentação por funcionalidade; M-01 networking com AuthStrategy explícito por chamada; M-02 persistence com SecureStore/LocalStore explicitamente separados | Aceita | VA-human | Registrado no Versus (architecture, id `997ba94a-...`); tabela completa em specs/technical/architecture.md (a criar) | P0005 |
| D0024 | 2026-08-29T16:31:36Z | Padrão arquitetural: Layered; princípios: SOLID + KISS/YAGNI + Composition over Inheritance; concorrência: Actor Model (Swift Concurrency) | Aceita | VA-human | Registrado no Versus (pattern, id `a83d0398-...`) | P0005 |
| D0025 | 2026-08-29T16:41:03Z | Lentes condicionais da Fase 2 ativadas: Resilience, UI/UX, Ethical/Human Impact, Process/Workflow, Observability/Operability, Linguistics/Grammar, Mechanical Engineering. Não ativadas (com motivo): Migration/Coexistence, Sustainability/Proportionality, Governance/Accountability, Control Engineering, Game Theory | Aceita | GA (análise técnica, sem objeção do VA-human) | Registrado via ferramenta estruturada `record_activated_lenses` (não prosa) | P0005/Fase 2 |
| D0026 | 2026-08-29T16:43:54Z | Crítica adversarial Iteração 1: 35 achados (8🔴/21🟡/6🟢) em specs/design/coverage-matrix.md; concentração identificada em cert-ext (6 lentes), networking e push-transport (5 lentes cada) | Registrada como achado, não decisão de produto | GA | Achados a resolver/aceitar na Fase 3 | Fase 2 |
| D0027 | 2026-08-29T16:46:51Z | Resposta unificada V(1)→V(2): 8 críticos resolvidos, 13 importantes resolvidos, 8 aceitos com justificativa, 6 sugestões adiadas para v2.0; 1 módulo novo (M-23 diagnostics); mudança estrutural 4,3% | Aceita | GA (resposta técnica), sem objeção do VA-human | specs/technical/architecture.md V(2) completa; stack atualizado (os_log/Logger, Firebase SDK versionado) | Fase 3 |
| D0028 | 2026-08-29T16:5xZ | Pós-ciclo 2↔3: recomendação de avançar para Fase 4 (4,3% mudança, 0 críticos) aceita pelo VA-human | Aceita | VA-human | Checklist anti-scope-creep verificado (4/4 ✅) | Fase 3 |
| D0029 | 2026-08-29T21:38:32Z | Fase 5 PAUSADA — ambiente sem Xcode (apenas Command Line Tools) impede verificação Automated-AV real de código Swift/iOS; VA-human optou explicitamente por pausar em vez de escrever código não verificável ou tentar instalar o Xcode | Aceita | VA-human (via AskUserQuestion, P0006) | Nenhum código Swift/iOS de produção foi criado nesta sessão. Registrado no Versus (process, id `7e4a7b28-...`) | P0006 |
| D0030 | 2026-08-29T21:41:12Z | Fase 5 RETOMADA — ambiente reverificado objetivamente: developer directory corrigido de /Library/Developer/CommandLineTools para /Applications/Xcode.app/Contents/Developer; Xcode 26.6 (Build 17F113); Swift 6.3.3; SDK iOS 26.5 (device+simulador) confirmado disponível via xcodebuild -showsdks | Aceita | VA-human (instrução direta) + GA (verificação objetiva) | Verificação Automated-AV agora genuinamente disponível; ver research/ENVIRONMENT.md, atualização P0007 | P0007 |
| D0031 | 2026-08-29T21:44:48Z | Método de verificação Automated-AV: xcodebuild -scheme ESJUDAC -destination 'platform=iOS Simulator,name=iPhone 17' build (não swift build com flags manuais, que produziu aviso de sysroot misturado) | Aceita | GA (técnica), tratado como achado de disciplina S6/S7, não ignorado | M-01 (networking): BUILD SUCCEEDED real, log completo em research/RESEARCH_LOG.md | P0007/Fase 5 |
| D0032 | 2026-08-29T21:57:44Z | Fase 5 pausada por escolha do VA-human em 7/23 módulos, para revisão do código antes de continuar | Aceita | VA-human | Nenhum módulo adicional iniciado; registrado no Versus (process, id `3810ff4f-...`) | Fase 5 |
| D0033 | 2026-08-29T22:59:54Z | APROVAÇÃO POST HOC de M-23 (diagnostics): VA-human aprovou o módulo como parte da V(2) após revisão isolada de 11 pontos, sem solicitar ajustes | Aceita (aprovação tardia, não retroativa) | VA-human | Fecha a lacuna de processo identificada em P0008. Registrado no Versus como decisão de Fase 5 (não reescreve a decisão original de Fase 3, id `7df01e3e-...`, 2026-08-29T16:46:51Z). Ver RESEARCH_LOG.md, entrada P0009, para a revisão completa | P0009 |
| D0034 | 2026-08-29T23:15:18Z | Prioridade imediata da Fase 5 concluída: M-04, M-08, M-09, M-10, M-11, M-12 implementados e compilados com sucesso real (xcodebuild); 13/23 módulos totais | Registrado como marco de progresso, não uma decisão de produto | GA (execução), sem objeção do VA-human até este ponto | M-11 usa RemoteMessagePayload agnóstico em vez de importar Firebase diretamente (decisão de implementação dentro do escopo do módulo, documentada em RESEARCH_LOG.md, não uma mudança de arquitetura) | P0010 |
| D0035 | 2026-08-29T23:22:02Z | Alvo de app iOS real criado: aplicativo ios/AppShell/ESJUDApp.xcodeproj, target único, iOS 17.0, bundle br.jus.tjac.esjud.app | Aceita | VA-human (aprovou a abordagem "novo .xcodeproj mínimo"); GA implementou | project.pbxproj autorado manualmente (sem gerador externo); xcodebuild BUILD SUCCEEDED real; app instalado e executado no simulador via xcrun simctl (PID real confirmado). Ainda sem dependência do Package local — próximo passo | P0010 |
| D0036 | 2026-08-29T23:24:40Z | Package local linkado ao App target: 13 produtos adicionados como packageProductDependencies de ESJUDApp | Aceita (continuação mecânica do plano já aprovado) | GA (execução) | xcodebuild -list resolveu o grafo (13 schemes de produto + ESJUDApp); xcodebuild build → BUILD SUCCEEDED real; app reinstalado/relançado no simulador (PID 40682) | P0010 |
| D0037 | 2026-08-29T23:41:35Z | M-13 app-shell + M-14 auth-ui implementados e verificados com evidência empírica real (compilação + execução + screenshot da tela de Login) | Registrado como marco de progresso técnico | GA (execução, dentro do escopo já aprovado); nenhuma decisão estrutural nova | Gap documentado no código: sem CertificateProviding real ainda (mTLS falha com .certificateUnavailable). Ver RESEARCH_LOG.md para detalhamento completo e descrição da screenshot | P0011 |
| D0038 | 2026-08-29T23:45:09Z | M-15 home-ui implementado e compilado; renderização visual pós-login NÃO verificada empiricamente (exigiria credenciais reais do Moodle de produção, que o GA não possui e não deve fabricar/adivinhar) | Registrado como marco + limitação honesta explícita | GA (execução); limitação reconhecida, não escondida | Ver RESEARCH_LOG.md para detalhamento; screenshot real confirma app funcional (tela de Login, estado não-autenticado) | P0011 |
| D0039 | 2026-08-29T23:49:47Z | Verificação E2E com backend real adiada — continuar Fase 5 verificando apenas compilação+execução de processo | Aceita | VA-human | Aplica-se a M-16..M-22; teste manual com credenciais reais fica para o VA-human ou Fase 6 | P0011 |
| D0040 | 2026-08-29T23:55:24Z | Todos os 22 módulos da arquitetura (M-01–M-22) implementados: M-17 a M-22 (certificates-ui, certidao-ui, cert-ext-ui, moodle-web-ui, news-ui, notifications-ui) nesta rodada | Registrado como marco | GA (execução) | Compilação conjunta real dos 22 targets do Package → BUILD SUCCEEDED. Gaps de escopo documentados nos arquivos (PDF viewer, modos ano/mês de Period, CourseReminders não lido por notifications-ui) | P0011 |
| D0041 | 2026-08-29T23:59:48Z | Falha de integração real encontrada e corrigida antes de compilar: placeholder sem sentido para token do Moodle e CPFs hardcoded sem via de edição | Corrigida (diagnóstico nomeado, S6/S7) | GA (autorrevisão) | LoginViewModel ganhou session capturada; CPF tornado editável em 3 telas; ESJUDApp.swift corrigido para usar dados reais da sessão | P0011 |
| D0042 | 2026-08-30T00:04:17Z | TODOS os 22 módulos + M-23 integrados em ESJUDApp.swift com navegação real (NavigationStack completo); app compilado e executado com sucesso real no simulador (PID 42022) | Marco de conclusão da Fase 5 (implementação) | GA (execução) | Screenshot real confirma renderização; ver RESEARCH_LOG.md para detalhamento completo | P0011 |
| D0043 | 2026-08-30T00:18:50Z | Revisão Adversarial de Escopo Original executada (tabela Promessa vs. Entrega, modo geração); 4 lacunas reais identificadas (mTLS, Firebase, course-reminders desconectado, site-info do Moodle); "CPF não capturado" descartado como falsa lacuna (nunca fez parte do login na baseline). VA-human decidiu continuar a Fase 5 para completá-las antes do Scope Inventory | Aceita | VA-human | Ordem: site-info → course-reminders → CertificateProviding real → Firebase (este último tratado como possível mudança estrutural, requer aprovação explícita antes de implementar) | P0011 |
| D0044 | 2026-08-30T00:23:46Z | 3 das 4 lacunas resolvidas: site-info do Moodle capturado, course-reminders conectado, CertificateProviding real (.p12 copiado ao bundle, carregado via SecPKCS12Import) | Aceita | GA (execução do plano D0043) | Compilação e execução reais confirmadas (PID 42671); .p12 confirmado fisicamente presente no bundle instalado via `ls`. 4ª lacuna (Firebase real) pendente de aprovação explícita | P0011 |
| D0045 | 2026-08-30T00:40:49Z | Tentativa de adicionar firebase-ios-sdk causou crash real e reproduzível do próprio Xcode (IDESwiftPackageCore); revertida, projeto restaurado a estado funcional | Registrada como falha de ferramenta, não de código | GA (diagnóstico) | Rede descartada como causa (HTTP 200 confirmado); ver RESEARCH_LOG.md para stack trace completo. Firebase real permanece débito técnico, decisão de próximos passos com o VA-human | P0011 |
| D0046 | 2026-08-30T00:50:38Z | Diagnóstico definitivo: Firebase SDK real incompatível com este ambiente CLI-only — presença no Package.swift local (mesmo sem produto linkado ao app) crasha o modelo de projeto do App target via XCLocalSwiftPackageReference | Registrada como conclusão de diagnóstico, não decisão de escopo | GA (2ª tentativa, causa raiz confirmada com precisão) | 3 tentativas reproduziram o mesmo crash exato; Package.swift e módulo experimental totalmente revertidos; caches de Package.resolved limpos; compilação real confirmada pós-reversão (Package + App) | P0011 |
| D0047 | 2026-08-30T01:04:29Z | Fase 5 pausada — VA-human tentará adicionar o Firebase manualmente via Xcode GUI, em vez de aceitar como débito técnico agora | Aceita | VA-human | Estado no momento da pausa: 22/22 módulos + M-23 implementados e compilando; 3/4 lacunas resolvidas; Scope Inventory formal ainda não executado | P0011 |
| D0048 | 2026-08-30T19:39:35Z | Verificação objetiva da integração manual do Firebase: DIVERGE do relatado — pacote resolvido de verdade, mas FirebaseMessaging NÃO linkado ao target, ninguém importa o SDK, e xcodebuild CLI trava (mesmo bug). Integração permanece PARCIAL, não concluída | Registrada como diagnóstico, não como conclusão de sucesso | GA (verificação objetiva por instrução do VA-human) | Não avançado ao Scope Inventory/Fase 6, conforme item 9/10 do pedido. Aguardando decisão do VA-human | P0012 |
| D0050 | 2026-08-31T00:29:25Z | Integração manual do Firebase via XCFrameworks (Opção A) concluída com evidência objetiva: FirebaseMessaging linkado/embutido no target ESJUDApp, xcodebuild real → BUILD SUCCEEDED, sem crash do IDESwiftPackageCore | Aceita (execução do plano já aprovado em D0049/P0015, sem mudança estrutural nova) | GA (execução); erro de concorrência Swift 6 corrigido via @preconcurrency nas conformances de AppDelegate, técnica padrão Apple para interoperar com Objective-C legado | Registrado no Versus (diagnosis, id `52c68c05-...`). Push real ainda não verificável — GoogleService-Info.plist real ausente do projeto, não fabricado; FirebaseApp.configure() falhará em runtime até um arquivo real ser fornecido pelo VA-human | (continuação de P0015) |
| D0051 | 2026-08-31T01:27:54Z | Dívida técnica registrada: push FCM real não pode ser validado nesta sessão por ausência de GoogleService-Info.plist real (dependência de acesso administrativo ao console Firebase institucional do TJAC, pertencente a terceiro) | Aceita | VA-human (declaração explícita) | Causa classificada como dependência externa/organizacional, não falha técnica. FirebaseMessaging integrado e projeto compilando (CONFIRMADO); inicialização real, token FCM e entrega de push permanecem NÃO VALIDADOS. Critério de push mantido PENDENTE no escopo, não removido. Registrado no Versus (scope, id `bc277887-...`) | (continuação de P0015) |
| D0052 | 2026-08-31T03:26:18Z | Smoke test P0 inicial FALHOU (bloqueante): app crashava no lançamento por FirebaseApp.configure() sem GoogleService-Info.plist válido — confirmação empírica real de D0051, com efeito colateral adicional (bloqueava todo o app, não só o push) | Registrada como falha real, não suavizada | VA-human (execução e relato do smoke test) | Corrigida via degradação graciosa: checagem objetiva de configuração antes de FirebaseApp.configure(); sem configuração válida, FCM é desabilitado e o resto do app segue normalmente, sem crash e sem fabricar arquivo. Rebuild real → BUILD SUCCEEDED; install/launch real no simulador → PID 8641 ativo sem crash (verificação do GA, não substitui smoke test do VA-human) | P0016/P0017 |
| D0053 | 2026-08-31T03:45:16Z | Smoke test P0 #2 FAIL bloqueante: erro real de rede (nw_endpoint_flow_failed_with_error / Socket is not connected) ao chamar api-esjud.tjac.jus.br (mTLS) após login; nome de usuário exibido incorretamente na Home | Registrada como falha real, causa diagnosticada antes de corrigir (S6) | VA-human (execução e relato do smoke test) | Verificação externa real (curl com o .p12 real) isolou a causa à camada URLSession do app iOS, não a rede/backend/certificado. Correção #1: MTLSSessionDelegate passou a conformar URLSessionTaskDelegate além de URLSessionDelegate. Correção #2 (achado por comparação com HomeScreen.kt): nome exibido na Home passou a usar session.fullName→session.username→"bem-vindo(a)" em vez do texto bruto do campo de login. Ambas dentro da arquitetura já aprovada, sem mudança estrutural. Rebuild real (Package + App) → BUILD SUCCEEDED em ambos os casos. Push FCM inalterado, permanece PENDENTE (D0051). Aguardando novo smoke test do VA-human | P0018 |
| D0054 | 2026-08-31T03:58:44Z | Smoke test P0 #3 FAIL: correção de D0053 (task-level delegate) não resolveu; causa raiz real identificada por reprodução isolada real: SecPKCS12Import falha (errSecAuthFailed -25293) ao importar cliente_api_esjud.p12, que usa criptografia PKCS12 legada ([cifra legada]), incompatível com a política atual do Security.framework da Apple | Registrada como falha real de correção anterior + diagnóstico definitivo, não suavizada | GA (instrumentação real + reprodução isolada fora do Simulator + corroboração externa via Apple Developer Forums #764516) | D0053 mantida no código (melhoria de robustez legítima, mas insuficiente) — não revertida nem apresentada como solução. Nenhuma alteração de TLS/trust/backend; nenhum certificado/segredo fabricado ou exposto (apenas metadados de algoritmo inspecionados via openssl -info). GA parado, aguardando decisão do VA-human sobre remediação (reencodar o .p12 com criptografia moderna vs. solicitar reemissão institucional) | P0019 |
| D0055 | 2026-08-31T04:04:56Z | CORREÇÃO de D0054: causa raiz real de SecPKCS12Import falhar não é [cifra legada], é senha vazia (confirmado por experimento controlado: [criptografia moderna] moderno + senha vazia ainda falha; mesmo material + senha real → sucesso, HTTP 200 real) | Registrada como correção explícita, D0054 não editada/apagada | GA (experimento controlado real antes de aplicar remediação) | .p12 reencodado a partir do MESMO par chave/certificado real (nenhum certificado fabricado) com senha aleatória forte + criptografia moderna; senha embutida em AppConfiguration.swift (mesmo padrão de baixo risco de D0015); arquivo original preservado como backup; temporários com chave privada em claro apagados imediatamente. Rebuild real (Package + App) → BUILD SUCCEEDED em ambos. mTLS ponta a ponta já confirmado fora do app; dentro do app aguarda smoke test do VA-human | P0019 (execução da opção "Reencodar o .p12 localmente" aprovada via AskUserQuestion) |
| D0056 | 2026-08-31T04:14:48Z | Smoke test P0 #4 FAIL; diagnóstico objetivo (sem nova correção): server trust e cadeia de certificados idênticos e válidos nos 3 hosts mTLS; hipótese de senha vazia (D0055) tratada como não confirmada para este sintoma; divergência real isolada entre ambiente Simulator/Xcode (challenge ClientCertificate não aparece) e reprodução isolada macOS (challenge aparece, mas api-esjud-registro-certificado-externo retorna NSURLErrorDomain -1206 mesmo com useCredential) | Registrada como diagnóstico objetivo, GA parado conforme instrução | GA (instrumentação SecTrustEvaluateWithError real + testes curl/openssl em todos os hosts + reprodução isolada) | Nenhum trust-all, nenhuma aceitação de cert inválido, nenhum backend alterado. Correção proposta (não aplicada): captura de handshake dentro do Simulator com o VA-human; contato institucional para comparar config de vhost de api-esjud-registro-certificado-externo especificamente | P0020 |
| D0057 | 2026-08-31T04:27:56Z | Smoke test P0 #5: diagnóstico separado por fluxo a partir de captura real de log no Simulator. Certidão/CertExt: causa CONFIRMADA (coalescência de conexão HTTP/2 entre hosts que compartilham o wildcard cert *.tjac.jus.br, ClientCertificate challenge nunca emitido, HTTP 421). Certificados: causa NÃO CONFIRMADA (limitação de instrumentação — sem path/decode logging; teoria de coalescência não se aplica, mesmo host que já funciona). AVA/Moodle modal: causa CONFIRMADA como distinta (chamada de rede subjacente teve sucesso; falha é em SFSafariViewController, fora do NetworkClient instrumentado) | Registrada como diagnóstico objetivo separado por fluxo, GA parado conforme instrução | GA (análise de captura real de log, sem nova execução/correção) | Os 3 problemas NÃO compartilham todos a mesma causa. Nenhuma correção aplicada. Log completo preservado no RESEARCH_LOG.md com trechos reais (host/authenticationMethod/status/NSError) | P0021 |
| D0058 | 2026-08-31T04:34:08Z | Frente 1 (Certidão/CertExt): correção mínima aplicada — URLSession isolada por host mTLS (sessionsByHost) em NetworkClient, evita coalescência HTTP/2 entre hosts com o mesmo wildcard cert | Aceita (execução da correção aprovada) | GA (execução), causa já confirmada em D0057 | Sem desabilitar HTTP/2, sem enfraquecer TLS, sem trust-all, sem alterar backend. Rebuild real → BUILD SUCCEEDED. Nota honesta: Certificado Externo pode ter causa adicional não totalmente explicada pela coalescência (ver D0056), a confirmar no próximo smoke test | P0022 |
| D0059 | 2026-08-31T04:34:08Z | Frente 2 (Certificados): apenas instrumentação de path/tipo esperado/bodyBytes/erro de decoding em NetworkClient — nenhuma correção por hipótese | Aceita | GA (execução) | Rebuild real → BUILD SUCCEEDED. Aguardando novo smoke test para diagnóstico objetivo antes de qualquer correção | P0022 |
| D0060 | 2026-08-31T04:34:08Z | Frente 3 (AVA/Moodle modal): apenas instrumentação (SFSafariViewControllerDelegate, logs de criação/apresentação/URL válida) — SFSafariViewController não substituído, fluxo não alterado. Achado por comparação com Android: MoodleWebScreen.kt usa Custom Tabs + onBack() imediato (não é modal sobreposto); iOS usa .sheet sobre a tela ainda presente na NavigationStack — divergência estrutural real, candidato a correlacionar | Aceita | GA (execução + achado de comparação) | Rebuild real → BUILD SUCCEEDED. Nenhum dado sensível logado (URL de autologin nunca logada por completo). Aguardando novo smoke test | P0022 |
| D0061 | 2026-08-31T05:28:24Z | Smoke test P0 #6: Frente 1 (D0058) CONFIRMADA resolvida — Certidão e Certificado Externo agora recebem ClientCertificate e HTTP 200 (sem mais 421). Diagnóstico objetivo das 3 falhas restantes: Certidão PARCIAL = geração OK, mas pdfBytes descartado (sem persistência/apresentação); Certificado Externo FAIL = listar() nunca chamado pela UI (lacuna de wiring, não rede/parsing); AVA/Moodle FAIL = decoding falhou (Moodle retornou erro lógico em HTTP 200) e iOS não tem o fallback que o Android tem (abrir login padrão do Moodle) | Registrada como diagnóstico objetivo, GA parado conforme instrução | GA (análise de captura real + leitura de código + comparação com baseline Android) | Cursos = PASS. Nenhuma correção aplicada nesta entrada. Causas das 3 falhas são todas DISTINTAS entre si (persistência ausente; wiring de UI ausente; fallback ausente) | P0023 |
| D0062 | 2026-08-31T05:33:36Z | Certidão de Curso corrigida: PDF gerado agora é salvo em Documents/certidoes/ e a tela oferece Abrir (QuickLook) e Compartilhar (ShareLink) | Aceita (execução da correção aprovada) | GA (execução) | API/formato de geração intocados; sem dependência nova. Rebuild real → BUILD SUCCEEDED | P0024 |
| D0063 | 2026-08-31T05:33:36Z | Certificado Externo corrigido: repository.listar()/.excluir() (M-07) conectados à UI (M-19) — nova seção "Certificados registrados" com swipe-to-delete condicionado a podeExcluir; buscarPessoa() agora chama carregarCertificados() automaticamente | Aceita | GA (execução) | Fluxos de buscar pessoa/registrar preservados; sem dependência nova. Rebuild real → BUILD SUCCEEDED | P0024 |
| D0064 | 2026-08-31T05:33:36Z | AVA/Moodle corrigido: novo MoodleWebRepository.loginPageURL() + fallback em MoodleWebViewModel.load() — falha lógica do autoLoginURL não é mais erro final, abre login padrão do Moodle (equivalente ao Android) | Aceita | GA (execução) | Sem alteração de backend/autenticação. Rebuild real → BUILD SUCCEEDED | P0024 |
| D0065 | 2026-08-31T05:43:48Z | Smoke test P0 #7: Certidão/Cursos/AVA-Moodle PASS (confirmam D0062/D0064). Certificados FAIL: causa confirmada por curl real = modelo Swift Certificate exige campo 'pessoa' inexistente na resposta real da API (Android tem a mesma expectativa divergente — achado de fonte desatualizada, não exclusivo do iOS). Certificado Externo FAIL: causa confirmada por curl real = CPF enviado sem máscara 000.000.000-00 que o backend exige; Android formata o CPF antes de chamar (formatarCpf), iOS não | Registrada como diagnóstico objetivo com confirmação empírica real, GA parado conforme instrução | GA (curl direto contra produção com o mesmo .p12, confirmando ambas as causas sem fabricar dados) | Nenhuma correção aplicada nesta entrada. Causas das duas falhas são distintas entre si (campo de modelo ausente na API vs. formato de CPF incorreto) | P0025 |
| D0066 | 2026-08-31T05:49:02Z | D0065-A implementada: campo pessoa removido de Certificate (M-05) — sem uso em nenhum lugar do código, removido com segurança. Validado contra a resposta real de produção (3045 bytes, idêntica à capturada no app): 15/15 certificados decodificados sem erro | Aceita | GA (execução + validação empírica) | Achado metodológico registrado: Certificate.kt (Android) também exige pessoa obrigatório, divergindo da API real — inconsistência preexistente de modelo/documentação vs. contrato real do backend, não erro exclusivo do iOS. Rebuild real → BUILD SUCCEEDED | P0026 |
| D0067 | 2026-08-31T05:49:02Z | D0065-B implementada: formattedCPF() adicionado a CertExtRepository (M-07, camada de domínio), aplicado em obterPessoa/listar/registrar/excluir; suporta CPF bruto e já formatado | Aceita | GA (execução + validação empírica) | Validado: helper produz 111.111.111-11 para bruto/formatado/com ruído; chamada real a obter_pessoa.php com CPF mascarado → HTTP 200 sucesso real. Rebuild real → BUILD SUCCEEDED. Confirmação dentro do app real (não apenas curl) e ausência de regressão em Certidão/Cursos/AVA-Moodle aguardam o próximo smoke test do VA-human — não fabricadas | P0026 |
| D0068 | 2026-08-31T16:21:38Z | Refinamento visual implementado nos 9 módulos de apresentação (AuthUI/HomeUI/CoursesUI/CertificatesUI/CertidaoUI/CertExtUI/NewsUI/NotificationsUI/MoodleWebUI), orientado por 8 screenshots reais do Android; criado Assets.xcassets (nunca existira) com logos reais e cor institucional | Aceita (execução do pedido de refinamento visual do VA-human) | GA (execução); nenhuma correção de bug, etapa puramente visual | Todos os 9 módulos + Package + App target recompilados individualmente → BUILD SUCCEEDED em todos os casos. Screenshot real do Login antes/depois confirma aproximação visual real com o Android. Nenhum contrato de backend/domínio alterado (única exceção: nomePessoa exposto publicamente no CertExtViewModel, binding necessário). Paridade visual NÃO declarada concluída — 8 das 9 telas aguardam validação humana real (login requer credenciais que o GA não possui) | P0027 |
| D0069 | 2026-08-31T16:32:27Z | Ajuste funcional pós-refinamento visual: card de notícia na Home agora navega diretamente para o detalhe (onSelectNoticia adicionado a HomeScreen, reaproveitando Route.materia já usada por NoticiasScreen); NoticiasScreen já estava correto desde P0027 | Aceita (execução do ajuste aprovado) | GA (execução) | Card inteiro tocável, sem clique intermediário; tela de detalhe (imagem/título/resumo/autor/data/conteúdo/compartilhar) preservada sem alteração. Nenhum backend/contrato/arquitetura alterado. Rebuild real (Package + App) → BUILD SUCCEEDED; build instalada no Simulator. Validação manual do toque ainda pendente do VA-human | P0028 |
| D0070 | 2026-08-31T17:00:25Z | Defeito real: sessão não persistia entre reaberturas do app — token ficava isolado no Keychain sem chave de "conta atual", e AppShell nunca chamava nenhuma restauração no startup. Achado adicional: logout() nunca era chamado por nenhum consumidor, deixando o Keychain intacto após "logout" | Registrada como defeito real + causa raiz + correção, não suavizada | GA (diagnóstico por leitura de código real + comparação com Android) | Corrigido: PersistedSessionMeta (não sensível) + chave de conta atual em LocalStore; AuthRepository.currentSession()/logout() novos; restauração no .task de startup do ESJUDApp; onLogout agora chama logout() de verdade. Nenhuma senha persistida em lugar algum. Rebuild real → BUILD SUCCEEDED (Package+App); instalação em estado limpo confirmada por screenshot real (cai em Login sem crash). Teste completo de restauração com credenciais reais pendente do VA-human — Fase 5 aberta até passar | P0029 |
| D0071 | 2026-08-31T17:03:43Z | Validação real (parcial) de D0070: restauração de sessão após fechar/reabrir o app CONFIRMADA por teste manual real (passos A-D). Passos E-G (logout limpa tudo; volta ao login) NÃO reportados como executados nesta rodada — aguardam confirmação humana explícita | Aceita | VA-human (execução real, parcial) | Push FCM mantido separado, PENDENTE (D0051), sem relação com este defeito | P0030 |
| D0072 | 2026-08-31T17:04:36Z | Reavaliação do critério de saída ui_runnable (Fase 5): mantido NÃO atendido — vários fluxos confirmados PASS reais (Login/Home/Cursos/Certidão/AVA-Moodle/restauração de sessão), mas Certificados/Certificado Externo (D0066/D0067) e navegação de notícia (D0069) ainda sem reconfirmação humana pós-correção; logout (E-G) sem confirmação humana | Registrada como reavaliação objetiva de gate, não decisão de produto | GA (get_exit_criteria/mark_exit_criterion via MCP Versus) | Fase 5 permanece aberta. Push FCM tratado separadamente (D0051), não bloqueia este critério por si só, mas push real também nunca foi validado | P0030 |
| D0073 | 2026-08-31T17:09:55Z | Smoke test final direcionado: PASS real para Certificados, Certificado Externo, navegação por toque em notícia (Home), logout explícito, e volta ao login após fechar/reabrir pós-logout — fecha todas as lacunas de P0030/D0072 | Aceita | VA-human (execução real completa) | Todos os fluxos P0 e correções D0057-D0072 agora confirmados dentro do app real. Push FCM mantido separado, PENDENTE (D0051) | P0031 |

## Fase 6 (Tests) — reconciliação retroativa das decisões D0074+ (feita em P0039; ver nota abaixo)

> As linhas D0074-D0081 abaixo foram escritas em 2026-09-05, reconstruindo
> de forma fiel decisões já registradas em tempo real no Versus
> (`record_decision`, ids reais citados) e em `research/RESEARCH_LOG.md`
> entre 2026-08-31T17:11Z e 2026-09-04. Esta tabela local ficou
> desatualizada durante a Fase 6 (achado F-PROC-07 registrado em P0036);
> os timestamps abaixo são os REAIS de cada decisão original (não a data
> desta reconciliação) — nenhum eles foi retroagido além do que já era o
> timestamp real da decisão no Versus/RESEARCH_LOG.

| D0074 | 2026-08-31T17:24:31Z | Plano de teste da Fase 6 aprovado pelo VA-human (P0032): tooling = XCTest unit no pacote SPM com rede mockada, sem automação de UI; specs/validation e specs/datasets populados antes do Test Map; teste manual = passada exploratória dedicada | Aceita | VA-human | Registrado no Versus (testing, id `938f1b14-...`). Suíte inicial: 67 testes, 0 falhas, run verde testemunhado pelo engine (`VERSUS_TEST_EXIT=0`, 2026-08-31T17:49:41Z) | P0032 |
| D0075 | 2026-08-31T18:11:59Z | Achado adversarial real da passada exploratória manual: UC-3 (Inscrição) FAIL — formulário não carrega. Causa raiz diagnosticada: fluxo de inscrição do iOS nunca captura o CPF do usuário (session.cpf estruturalmente vazio; sem CpfInputDialog equivalente ao Android); backend responde HTTP 400 "parâmetros obrigatórios" | Registrada como achado, GA parado conforme instrução (diagnosticar antes de corrigir) | VA-human (relato do FAIL) + GA (diagnóstico com curl mTLS real) | UC-2/UC-3 não marcados PASS. Registrado no Versus (testing, id `e7e89c79-...`) | P0033 |
| D0076 | 2026-08-31T18:24:10Z | Correção aplicada para D0075: EnrollmentViewModel ganhou captura/validação/persistência de CPF (cpfDigits, hasValidCPF, sanitizeCPF, persistCPF); extractMessage passou a decodificar JSON antes do fallback regex | Aceita (aprovada "como proposta") | VA-human (aprovação) + GA (execução) | 74 testes, 0 falhas, engine-witnessed. Reteste manual do VA-human: AINDA FALHOU (ver D0077) | P0033 |
| D0077 | 2026-08-31T19:36:37Z | 2º achado adversarial UC-3: FAIL persiste ("Erro ao carregar formulário") mesmo após D0076. Diagnóstico: sub-orçamento de timeout do cliente iOS (30s) vs. baseline Android (OkHttp readTimeout 120s) para o mesmo endpoint Puppeteer-backed; defeito latente secundário: FormularioInscricao mais estrito que o modelo Android | Registrada como achado, GA parado conforme instrução | VA-human (relato do FAIL) + GA (diagnóstico com curl mTLS real + leitura de NetworkModule.kt) | Nenhuma correção causal aplicada ainda. Registrado no Versus (testing, id `6bf55d98-...`) | P0034 |
| D0078 | 2026-08-31T22:50:22Z | VA-human aprovou SOMENTE a mudança A (observabilidade) das 4 propostas para D0077: NetworkError deixa de colapsar em mensagem genérica — cada modo (transport/timeout, decoding, httpStatus+message, certificado, URL inválida) vira mensagem distinta e seguindo sem CPF/PII; NetworkClient.transport passa a logar só domain/code, nunca a URL com o cpf | Aceita (parcial — só a mudança A; B/C/D adiadas) | VA-human | 78 testes, 0 falhas, engine-witnessed. Timeout/modelo/lifecycle do EnrollmentViewModel explicitamente NÃO alterados por instrução | P0035 |
| D0079 | 2026-09-04T19:40:23Z | 3º achado adversarial UC-3: sintoma MUDOU — loading infinito, nenhuma mensagem de erro (não mais "Erro ao carregar formulário"). Instrumentação adicionada (URLSessionTaskMetrics sanitizadas, didCreateTask, watchdog de timeout observacional, instanceID + 5 eventos de lifecycle em EnrollmentViewModel) — SEM correção, por instrução explícita | Registrada como achado + instrumentação, GA parado conforme instrução | VA-human (relato do novo sintoma) | Candidatos identificados por análise estática (continuation/delegate/retry/stream/await duplo descartados; recriação de ViewModel e semântica de timeout-por-inatividade não descartáveis sem evidência real). 78 testes, 0 falhas | P0037 |
| D0080 | 2026-09-04T20:05:58Z | Avaliação de evidência parcial real (captura de log mostrou -999/NSURLErrorCancelled em /api/cursos, não em /[endpoint-formulario-inscricao]): não confirma nem refuta a causa do UC-3 diretamente, mas reforça a plausibilidade da hipótese de recriação de ViewModel (CoursesViewModel usa o mesmo padrão de construção inline que EnrollmentViewModel) | Registrada como avaliação de evidência, sem correção | GA (avaliação) | Entregue roteiro de captura precisamente priorizado (10 âncoras textuais + regras de correlação por instanceID/taskIdentifier) para a próxima tentativa | P0038 |
| D0081 | 2026-09-05T00:21:35Z | CAUSA RAIZ CONFIRMADA do UC-3 (investigação ponta-a-ponta, P0039): `EnrollmentScreen`/`CoursesScreen` guardavam seus ViewModels como propriedade simples (`@Bindable`, sem `@State`) — `ESJUDApp.destination(for:)` constrói uma instância NOVA a cada reavaliação do closure de `navigationDestination`; sem `@State`, essa instância substitui a anterior, cancelando a `Task` de `fetchForm()`/`load()` em andamento. Evidência convergente: (a) leitura de código descarta continuation/delegate/retry/stream/await-duplo como causa; (b) `-999` real já observado em `/api/cursos` (mesmo padrão de construção); (c) padrão de perda de estado por objeto `@Observable` construído inline em `navigationDestination` sem `@State`/`@StateObject` é um comportamento documentado em fóruns da Apple e na comunidade SwiftUI; (d) comparação com a baseline Android — `viewModel()` do Android já resolve este problema por desenho (ViewModel escopado ao ciclo de vida da entrada de navegação), então o defeito é uma divergência introduzida pela porta iOS, não algo replicado do Android; (e) curl real confirma que o backend responde de forma limpa e completa (sem hang de conexão) para o caso de sucesso, reduzindo a plausibilidade de uma causa exclusivamente de rede/timeout | Aceita — correção aplicada | GA (investigação + correção), autorizado explicitamente pelo VA-human a corrigir quando a causa estivesse objetivamente identificada | `EnrollmentScreen`/`CoursesScreen` passaram a guardar seus ViewModels via `@State` (semeado uma única vez via `State(initialValue:)`). Mensagem de cancelamento (`-999`) diferenciada de timeout (evita mascarar). 79 testes, 0 falhas, engine-witnessed; Package e App target compilando. UC-2/UC-3 continuam SEM PASS até reteste manual real (S4) | P0039 |
| D0082 | 2026-09-05T20:17:12Z | CAUSA RAIZ REAL do UC-3 (loading infinito) encontrada e corrigida (P0040): não era rede/timeout/cancelamento/recriação de ViewModel — era a MÁQUINA DE ESTADOS do `EnrollmentScreen.body`. O ramo `case .idle, .loading:` mostrava `ProgressView` sempre que `hasValidCPF == true`; ao completar os 11 dígitos do CPF, a tela trocava para o spinner e removia o `cpfEntry` (que contém o botão "Carregar formulário"), sem jamais chamar `loadForm()` e sem saída. Evidência decisiva do VA-human: `enrollment_vm_init` aparece no console, `loadForm_started` NUNCA aparece. A correção de P0039 (@State) expôs o defeito ao fazer o CPF digitado persistir de forma estável. Correção causal mínima: `case .idle` e `case .loading` separados — `.idle` sempre mostra `cpfEntry` (botão habilita em 11 dígitos, ação explícita, fiel ao `CpfInputDialog` do Android que não dispara sozinho); `.loading` só quando `loadForm()`/rede real está em andamento. Novo `isLoadingForm { formState == .loading }` — UI de loading nunca mais derivada de `hasValidCPF`. `.task` de auto-load mantido só para o caso de CPF pré-preenchido | Aceita — correção aplicada | GA (investigação da máquina de estados + correção), autorizado pelo VA-human a corrigir na mesma investigação | Testes VAL-45 (EnrollmentViewModelTests): `formState` começa `.idle` e completar o CPF não muda isso; `loadForm()` sempre sai de `.loading` para terminal nos 7 desfechos. 81 testes, 0 falhas, engine-witnessed; Package e App target compilando (exit 0). O `body` da View em si não é coberto por unit test (escopo P0032, sem automação de UI). UC-2/UC-3 continuam SEM PASS até reteste manual real (S4). Registrado no Versus (architecture, id a confirmar) | P0040 |
| D0083 | 2026-09-06T00:07:00Z | CERTIFICADOS (UC-4) — 2 defeitos reais encontrados na passada exploratória (P0042) e corrigidos. D1: `CertificatesViewModel.downloadState` era UM estado compartilhado renderizado em TODOS os `CertificateCard` do `ForEach` — tocar em um certificado punha `.downloading` e os 16 cartões mostravam o spinner (ilusão de "baixa vários ao mesmo tempo"; só UMA `URLSessionTask` rodava — confirmado por análise + curl). D2: `download()` fazia `case .success:` SEM `let data` — buscava os bytes do PDF e os DESCARTAVA; salvar arquivo e apresentar (QuickLook/ShareLink) NUNCA foram implementados (gap documentado no cabeçalho e em D0040; o sibling CertidaoScreen tem o pipeline desde D0062). curl real confirma que o backend devolve `application/pdf` binário completo (375 KB, `%PDF-1.4`, HTTP 200, sem redirect). Comparação Android: `CertificatesViewModel.kt` salva em `getExternalFilesDir/certificados/…pdf` e `CertificatesScreen.kt` abre via `FileProvider`+`ACTION_VIEW` — etapas ausentes no iOS. CORREÇÃO (Sources/CertificatesUI/CertificatesScreen.swift): estado de download por índice (`downloadStates[Int]`/`downloadState(for:)`, cada cartão observa só o seu; guard contra toque repetido no mesmo item); `download()` salva o PDF em `Documents/certificados/certificado-<n>-<slug>.pdf` (arquivo real, `.pdf`, SEM CPF no nome), expõe `downloadedURLs[index]` (Abrir/ShareLink) e `lastDownloaded` (abre QuickLook automaticamente). Adicional: `NetworkClient.redactedPathForLog` mascara CPF de 11 dígitos / `000.000.000-00` no `path` ANTES de qualquer `os_log` (o endpoint `/api/certificados/{cpf}/...` estava vazando CPF no log). Endpoints/API não alterados; sem trust-all; sem dependência nova. | Aceita — correção aplicada | GA (investigação ponta-a-ponta + correção), autorizado pelo VA-human a corrigir quando a causa estivesse objetivamente confirmada | Testes VAL-46 (CertificatesViewModelTests, 4): estado por item (download de A não afeta B); PDF salvo em arquivo real existente com tamanho == bytes e sem CPF no nome; falha isolada sem abrir preview; `load()` reseta estados. 85 testes, 0 falhas, engine-witnessed; Package e App target compilando (exit 0). A apresentação QuickLook/ShareLink na View não é coberta por unit test (escopo P0032). UC-4 continua SEM PASS até reteste manual real (S4). Registrado no Versus (architecture, id a confirmar). | P0042 |
| D0084 | 2026-09-06T00:26:54Z | Reteste manual da Fase 6 (P0043). (A) CERTIFICADOS / UC-4: reteste manual real do VA-human apos a correcao P0042/D0083 → listagem PASS, download PASS, abertura/visualizacao do PDF PASS. UC-4 marcado PASS manual. (B) INSCRICAO EM CURSO / UC-3: o VA-human validou a mesma funcionalidade na baseline Android e confirmou que o formulario de inscricao tambem esta indisponivel la. RECLASSIFICACAO por paridade: a replicacao iOS esta CORRETA em relacao a baseline (mesmo endpoint GET /[endpoint-formulario-inscricao], mesmos parametros — CPF de 11 digitos crus, este endpoint nao usa mascara —, mesmo mapeamento 422→indisponivel, mesma mensagem exibida). Evidencia comum de causa de backend: varredura curl de P0041 (cliente HTTP distinto do URLSession) → 422 FORM_UNAVAILABLE 'etapa: timeout' DETERMINISTICO para 3 de 6 cursos abertos ([tempo determinístico observado], Puppeteer do backend estourando o proprio tempo), HTTP 200 com formulario completo para 2 de 6. O comportamento no iOS NAO e regressao da porta; o backend (api-esjud.tjac.jus.br, contrato fixo D0012) e a causa externa comum; UC-3 NAO permanece FAIL por defeito do cliente. Nova correcao no cliente iOS: NAO aplicada (nao mascarar defeito compartilhado). Dependencia externa registrada separadamente como EXT-DEP-02 (analoga a EXT-DEP-01 = push FCM / GoogleService-Info.plist, D0051). (C) UC-2 (listar cursos): paridade — funcional em ambos. | Aceita | VA-human (execucao e validacao comparativa reais) | UC-4 PASS manual; UC-2 PASS (paridade); UC-3 paridade mantida + EXT-DEP-02, nao e FAIL do cliente. manual_testing/edge_cases permanecem met=false — a passada exploratoria NAO esta completa (faltam UC-1/5/6/7/8/9/10/11/12 e edge cases do roteiro P0032). Fase 6 aberta so por isso. Nao avanca para a Fase 7. Nenhuma alteracao de codigo. Registrado no Versus (testing, ids a confirmar). Test Map (specs/validation/criterios-de-validacao.md) atualizado. | P0043 |
| D0085 | 2026-09-06T00:52:00Z | Passada exploratoria manual da Fase 6, BLOCO A (auth/sessao) — execucao manual real do VA-human com credenciais reais: UC-1 Login PASS (abrir sem sessao sem crash; campos vazios nao enviam; senha errada → mensagem clara e sem sessao; credenciais validas → Home com nome real do perfil; Home renderiza), UC-11 Logout PASS (logout volta ao Login; sem acesso a telas internas; relogin sem vazamento de dados anteriores), UC-12 persistencia/restauracao de sessao PASS (forca-fecha logado → reabre logado; logout + forca-fecha → reabre no Login). | Aceita | VA-human (execucao manual real) | UC-1/UC-11/UC-12 marcados PASS manual. Confirma por execucao humana (S4) VAL-1/2/3/4/5/6 (incl. a metade Keychain que so o teste manual cobre). manual_testing/edge_cases PERMANECEM met=false — BLOCOS B (UC-5/UC-6), C (UC-7/UC-8/UC-9) e D (UC-10 + edge cases) pendentes. Registrado no Versus (testing, id a confirmar). Test Map (specs/validation/criterios-de-validacao.md) atualizado com nota de validacao manual. | P0044 |
| D0086 | 2026-09-06T01:03:00Z | Passada exploratoria Fase 6 BLOCO B. UC-5 Certidao PASS manual (gerar → Abrir PDF QuickLook → Compartilhar; Validar OK). UC-6 Certificado Externo B5 FAIL PARCIAL manual: ausencia do seletor Servidor/Magistrado no iOS — lacuna real de paridade INTRODUZIDA na porta (a baseline Android mostra `showTipoDialog` antes de entrar). Causa: ESJUDApp.swift abria Route.certExt(.servidor) hardcoded; o modo .magistrado ja existia por completo (dominio M-07, prefixo de rota vida-funcional, campos Atuacao/Periodo no ViewModel/Screen) mas inalcancavel pela navegacao. CORRECAO MINIMA (sem nova arquitetura): HomeScreen.onOpenCertExt virou (magistrado: Bool) -> Void e o card abre um .confirmationDialog nativo Servidor/Magistrado (espelha o Android); ESJUDApp roteia Route.certExt(magistrado ? .magistrado : .servidor). Toda a cadeia a jusante ja existia, nao foi tocada. Fluxo Servidor: identico ao anterior, sem regressao. Fluxo Magistrado: mesma tela + campos Atuacao/Periodo, submit() envia nome_atuacao/nome_periodo, rota vida-funcional. | Aceita — correcao aplicada | GA (correcao), autorizado pelo VA-human na propria mensagem (B5 FAIL + requisitos da correcao) | Testes VAL-47: CertExtViewModelTests (novo; tipo fixo por instancia; Magistrado envia atuacao/periodo + rota vida-funcional; Servidor omite + rota de servidor) + CertExtTests.test_registrar_magistrado_fields_gated_by_tipo (novo) + test_route_prefix_depends_on_tipo (existente). O .confirmationDialog SwiftUI nao e coberto por unit test (escopo P0032). 89 testes, 0 falhas, engine-witnessed; Package e App target compilando (exit 0). UC-6 CONTINUA SEM PASS ate reteste manual de B5-B10. Fase 6 aberta; manual_testing/edge_cases met=false; BLOCOS C/D pendentes. Registrado no Versus (architecture, id a confirmar). Test Map atualizado (VAL-47 + coluna de status por caso de uso). | P0044 |
| D0087 | 2026-09-06T01:12:00Z | Reteste manual BLOCO B / UC-6 Certificado Externo apos a correcao D0086: B5 PASS (dialogo Servidor/Magistrado/Cancelar aparece), B6 PASS (Servidor busca CPF, exibe identificacao, carrega lista), B7 PASS (anexo PDF confirmado, botao Registrar habilita), B8 PASS (registro com confirmacao explicita do backend), B9 PASS (item em analise pode excluir; item fora de analise nao oferece excluir), B10 PASS (Magistrado exibe Atuacao e Periodo e registra corretamente). UC-6 marcado PASS manual. Trilha preservada: B5 FAIL original (lacuna de paridade — sem seletor Servidor/Magistrado) → causa (Route.certExt(.servidor) hardcoded; modo Magistrado inalcancavel) → correcao (.confirmationDialog + roteamento por Bool; VAL-47; 89 testes verdes) → reteste PASS. | Aceita | VA-human (execucao manual real) | manual_testing/edge_cases PERMANECEM met=false — BLOCOS C (UC-7/8/9) e D (UC-10 + edge cases) pendentes. Status: UC-1/2/4/5/6/11/12 PASS; UC-3 paridade + EXT-DEP-02. Nenhuma alteracao de codigo. Registrado no Versus (testing, id a confirmar). Test Map atualizado. | P0044 |
| D0088 | 2026-09-06T01:52:57Z | INTEGRACAO REAL do GoogleService-Info.plist e caminho FCM ponta-a-ponta no cliente iOS (P0045). Arquivo institucional real recebido do TJAC em 2026-09-05 (BUNDLE_ID=br.jus.tjac.esjud.app confere; valores sensiveis nunca expostos). (1) plist movido para AppShell/ESJUDApp/ e adicionado ao target ESJUDApp / Copy Bundle Resources (verificado no bundle apos build). (2) FirebaseMessagingBridge.swift reescrito — AppDelegate @MainActor: requestAuthorization([.alert,.badge,.sound]) sempre (tambem p/ lembretes locais M-12, como o POST_NOTIFICATIONS do MainActivity) + registerForRemoteNotifications; didRegister...DeviceToken → evidencia SANITIZADA do token APNs (bytes+fp SHA-256) + Messaging.apnsToken; didFailToRegister...WithError → log domain/code com nota de que falha no Simulador sem entitlement e esperada; didReceiveRemoteNotification async → onRemoteMessage + .newData (silent/background); didReceiveRegistrationToken → evidencia sanitizada do token FCM + onTokenRefresh + re-registro do device no painel se ha sessao (espelha onNewToken Android); willPresent async → onRemoteMessage + banner (foreground); didReceive response (TAP) async → onRemoteMessage (dedup por id) + onOpenNotifications (divergencia deliberada do Android: leva a tela de Notificacoes do app em vez de abrir actionUrl no browser — evita duplicar o allowlist tjac.jus.br e reduz open-redirect). hasValidFirebaseConfiguration() mantido SO como protecao. (3) Sources/PushTransport/PushInterop.swift (novo, agnostico de SDK): RemoteMessagePayload.init(userInfo:) (parser movido do app-shell p/ ser testavel; mesmo contrato de chaves do onMessageReceived Android) + PushTokenEvidence.fingerprint (SHA-256 prefixo 4 bytes + comprimento; NAO reversivel; nunca contem o token). (4) ESJUDApp.swift: .onAppear injeta onOpenNotifications + currentSession; registerPushDeviceIfLoggedIn() chamado em restauracao de sessao, onLoggedIn e scenePhase==.active (paridade com LaunchedEffect(session) do MainActivity). (5) entitlements/Info.plist — ver D-nota abaixo. (6) Package.swift: PushTransport add as deps de DomainTests. BACKEND: nenhum novo — PushTransportRepository.registerDevice ja reproduzia o contrato do PainelRepository.registerDevice Android; o que faltava eram as CHAMADAS (nunca invocadas no iOS antes). | Aceita — integracao aplicada | GA (implementacao), autorizado pelo VA-human na propria mensagem (P0045: "faca a integracao real ... feche o fluxo FCM de ponta a ponta") | npm test (xcodebuild test ESJUDAC-Package) → 96 testes, 0 falhas, ** TEST SUCCEEDED **, engine-witnessed (VERSUS_TEST_EXIT=0); +7 PushTransportTests. Package build → BUILD SUCCEEDED. App target (AppShell/ESJUDApp.xcodeproj) xcodebuild clean build → ** BUILD SUCCEEDED ** (1 warning intermediario — nome do metodo didFail — corrigido; rebuild limpo). Sem trust-all; sem PII/valores sensiveis do plist em log; bundle id inalterado; plist real nao fabricado. UC-9 NAO marcado PASS (compilar/rodar teste nao e prova de push real). Fase 6 aberta; manual_testing/edge_cases met=false; NAO avanca para a Fase 7. Registrado no Versus (architecture, id 0b241340-d071-4225-8189-f785cb75ce65). | P0045 |
| D0089 | 2026-09-06T01:53:17Z | EXT-DEP-01 (push FCM real / GoogleService-Info.plist) — TRANSICAO DE ESTADO, historico preservado (linha original em FINDINGS_CURRENT.csv intacta; nova linha EXT-DEP-01-UPD1). ANTES: BLOQUEADO (D0051 — arquivo so geravel no console Firebase institucional, indisponivel ao VA-human). AGORA: DESBLOQUEADO PARCIALMENTE — arquivo real integrado (D0088); caminho de cliente iOS completo e verificavel por build/teste; prova ponta-a-ponta (push real no dispositivo) pendente e dependente SO de acoes humanas. ACHADO REAL sobre entitlements: sob a assinatura atual (ad-hoc "Sign to Run Locally", sem team) o Xcode reduz o ESJUDApp.entitlements a .xcent VAZIO — aps-environment so vale com assinatura institucional (App ID com Push + provisioning). O arquivo foi mantido, correto e INERTE ate la (respeita "nao inventar entitlement se a assinatura nao suportar"). UIBackgroundModes=[remote-notification] via Info.plist parcial + INFOPLIST_FILE (merge com GENERATE_INFOPLIST_FILE=YES confirmado no bundle). ACOES HUMANAS OBRIGATORIAS para fechar UC-9 (nenhuma executavel pelo GA): (1) dispositivo fisico iOS (Simulador nao exercita APNs/FCM reais); (2) conta Apple Developer institucional TJAC — App ID br.jus.tjac.esjud.app com Push Notifications, provisioning com aps-environment, assinar com o team; (3) APNs Auth Key .p8 (Key ID + Team ID) no Firebase Console → Cloud Messaging → Apple app configuration (ou par de certificados APNs); (4) envio de push de teste pelo Firebase Console e/ou pelo backend do painel; (5) validacao manual S4 no device real (token APNs+FCM nos logs sanitizados; device no painel; foreground → banner sem duplicar; background/terminado → bandeja; toque abre a tela de Notificacoes; polling /api/public/feed segue funcional; badge/"marcar todas" coerentes; sem duplicacao push x feed). | Aceita — registrada como transicao de estado + lista de acoes humanas | GA (registro), a partir do fato reportado pelo VA-human (recebimento do arquivo) | UC-9 permanece SEM PASS; manual_testing/edge_cases met=false; Fase 6 aberta; sem avanco para a Fase 7. EXT-DEP-01 deixa de ser "bloqueio por indisponibilidade de arquivo" e passa a ser "pendencia de validacao de runtime em dispositivo real com credenciais institucionais". Registrado no Versus (testing, id 3354d6a5-ab0c-47c4-a08b-9609827a0d57). | P0045 |
| D0090 | 2026-09-06T02:11:54Z | UC-9 Notificações — classificação de estado + nova dependência externa **EXT-DEP-03**. Sem alteração de código. FATO (VA-human): não há Apple Developer Team institucional (TJAC) no Xcode desta máquina; o app exige essa Team para assinatura/provisionamento; sem ela `aps-environment` não entra no provisioning (confirmado em P0045/D0089 — `.xcent` sai vazio sob assinatura ad-hoc sem team); logo registro APNs + validação de push real em dispositivo físico não podem ser concluídos agora. CLASSIFICAÇÃO: não é defeito do código iOS, não é FAIL do cliente — é pendência técnica/externa de validação do ecossistema institucional. PRESERVADO (D0088): `GoogleService-Info.plist` real integrado; Firebase init com config real; Messaging.delegate; registro/refresh de token FCM com evidência sanitizada; contrato backend `register-device` (paridade Android) com chamadas ligadas; handlers foreground/background/tap; `UIBackgroundModes remote-notification`; `ESJUDApp.entitlements` `aps-environment` inerte até assinatura; +7 PushTransportTests; 96 testes verdes engine-witnessed; Package + App target BUILD SUCCEEDED. Falta SOMENTE validação de runtime real com assinatura institucional + APNs. STATUS UC-9 = "IMPLEMENTAÇÃO CONCLUÍDA / VALIDAÇÃO REAL PENDENTE POR DEPENDÊNCIA INSTITUCIONAL" — não PASS, não FAIL. A passada exploratória da Fase 6 NÃO fica bloqueada por isto — segue para o BLOCO D. EXT-DEP-03 = "Apple Developer Team / provisioning APNs institucional", distinta de EXT-DEP-01 (arquivo plist, já resolvido 2026-09-05). Cadeia: EXT-DEP-01 DESBLOQUEADO; EXT-DEP-03 ABERTO (Team Apple + App ID com Push + provisioning com aps-environment + APNs Auth Key .p8 no Firebase Console + envio de push de teste + validação S4 em device real). Ambos → Fase 7 como limitação do ecossistema, não defeito do produto. | Aceita — classificação e registro (sugestão do próprio VA-human na mensagem) | VA-human (fato + classificação) + GA (registro) | Sem alteração de código. `manual_testing`/`edge_cases` met=false; Fase 6 aberta; sem avanço para a Fase 7. RESEARCH_LOG (P0046), Test Map (linha UC-9 + EXT-DEP-03), FINDINGS_CURRENT.csv (nova linha EXT-DEP-03; EXT-DEP-01/EXT-DEP-01-UPD1 preservadas), GATES.md atualizados. Registrado no Versus (testing, id f814227e-9c18-4d24-b7ed-a9ab35db27d2). | P0046 |
| D0091 | 2026-09-06T02:33:01Z | CONSOLIDAÇÃO FINAL da validação funcional da Fase 6 (P0047). Sem alteração de código; nenhum critério de saída marcado (aguarda VA-human). **Matriz de casos de uso (estado manual real):** UC-1/2/4/5/6/7/8/11/12 = **PASS manual** confirmado pelo VA-human ao longo de P0043–P0046 (D0084–D0090); UC-3 = **paridade com a baseline**, cliente correto, funcionalidade bloqueada por EXT-DEP-02 (não é FAIL do cliente, não reaberto); UC-9 = **implementação concluída / validação runtime real pendente** por EXT-DEP-03 (não PASS, não FAIL do cliente); UC-10 = lógica de domínio coberta por teste automatizado (VAL-34..38) + porte Tier-2 fiel do Android, mas a perna de **entrega da notificação local** não foi verificada manualmente — item aberto: check curto OU waiver do VA-human. **Edge cases:** cobertura acumulada organicamente na passada (achados adversariais reais corrigidos com regressão: UC-3 máquina de estados P0037–P0040/VAL-44/45; Certificados P0042/VAL-46; CPF em log P0042; seletor CertExt P0044/VAL-47) + caminhos automatizados (rejeições 400/409/422 sem PII, hasValidCPF, PDFAttachment, dedup push×polling, cadência de lembretes). **Critérios de saída (avaliação objetiva IACDM):** `tests_passing` MET (96/96 verde engine-witnessed, 2026-09-06T01:57:12Z; suíte 67→78→96 na fase, sem regressão); `manual_testing` substancialmente completo (9/12 UCs PASS + UC-3 paridade + UC-9 cliente-completo; pendência = UC-10 perna de entrega); `edge_cases` cobertura substancial de fato, marcável met como julgamento do VA-human com a racional registrada. **Proposta:** se o VA-human (a) confirmar UC-10 (check curto ou waiver) e (b) aceitar a racional de edge_cases → marcar `manual_testing`+`edge_cases` met e FECHAR a Fase 6 COM RESSALVA EXPLÍCITA: EXT-DEP-02 (UC-3, backend/Puppeteer) e EXT-DEP-03 (UC-9, Apple Developer Team / provisioning APNs institucional) — dependências externas do ecossistema comuns à baseline Android, carregadas para a Fase 7 como limitações do estudo, não defeitos do produto. EXT-DEP-01 (arquivo plist) resolvido 2026-09-05. | Proposta — aguarda confirmação do VA-human | GA (consolidação e avaliação objetiva) — decisão de fechamento e waiver de UC-10/edge_cases pertencem ao VA-human | Nenhum `mark_exit_criterion`/`advance_phase` executado. RESEARCH_LOG (P0047), Test Map (matriz UC consolidada + seção "Consolidação final da Fase 6" + critérios de saída), GATES.md, FINDINGS_CURRENT.csv atualizados. Registrado no Versus (testing, id 5f59ade4-67fb-402d-a2cd-a3db3bc99393). | P0047 |
| D0092 | 2026-09-06T02:44:54Z | REFINAMENTO da classificação do UC-9 (referência D0091/D0090/D0088). Esclarecimento do VA-human: a funcionalidade de Notificações no app **não** está genericamente pendente. **Split:** (a) tela de Notificações + listagem de avisos + polling `/api/public/feed` + badge de não lidas + marcar todas como lidas = **PASS manual** (execução humana real); (b) sub-funcionalidade **push remoto APNs/FCM** = **implementação concluída / validação runtime pendente por EXT-DEP-03** (cliente concluído D0088; `GoogleService-Info.plist` real integrado; testes/build verdes; entrega real não validável sem Apple Developer Team institucional do TJAC + provisioning com `aps-environment`). NÃO classificar o UC-9 inteiro como sem validação — só a sub-parte de push remoto é pendência externa. Não impede o fechamento da Fase 6, desde que a ressalva EXT-DEP-03 permaneça explicitamente registrada e carregada para a Fase 7. **Efeito:** UCs com PASS manual passam de 9/12 para **10/12** (UC-1/2/4/5/6/7/8/9/11/12). Itens ainda abertos para fechar a Fase 6 (inalterados): UC-10 (check curto ou waiver) + aceitação da racional de `edge_cases` pelo VA-human. | Aceita — refinamento de classificação (esclarecimento do próprio VA-human) | VA-human (esclarecimento) + GA (registro) | Nenhum `mark_exit_criterion` / `advance_phase` executado. RESEARCH_LOG (P0047 cont.), Test Map (linha UC-9 + seção Consolidação final), GATES.md, FINDINGS_CURRENT.csv atualizados. Registrado no Versus (testing, id 0fae1e13-ed83-496a-b1e2-827fe841313a). | P0047 |
| D0093 | 2026-09-06T03:33:51Z | AJUSTE FINAL da classificação do UC-9 (referência D0092/D0091/D0088). Esclarecimento adicional do VA-human: a funcionalidade de notificações administrativas **já funciona em runtime** — aviso enviado aparece no app; toque no ícone de Notificações mostra o aviso na lista; feed/polling/badge/tela operacionais. **Classificação definitiva:** (a) feed/listagem/polling `/api/public/feed`/badge/marcar como lidas = **PASS manual**; (b) **recebimento da notificação dentro do app** (aviso enviado → lista via feed/polling) = **PASS manual**; (c) **push remoto do sistema iOS (APNs/FCM)** — banner na Central de Notificações, recebimento em background/encerrado, tap abrindo o app/tela = **implementação concluída / validação runtime pendente por EXT-DEP-03** (falta Apple Developer Team institucional do TJAC + provisioning com `aps-environment`). A pendência é **exclusivamente o canal push remoto do sistema iOS**; não classificar o UC-9 inteiro como incompleto nem "recebimento de notificações" como pendente genérico. Não bloqueia o fechamento da Fase 6. Contagem inalterada (10/12 PASS manual). Itens abertos para fechar a Fase 6 inalterados: UC-10 (perna de entrega da notificação local) + aceitação da racional de `edge_cases`. | Aceita — ajuste de classificação (esclarecimento do próprio VA-human) | VA-human (esclarecimento) + GA (registro) | Nenhum `mark_exit_criterion` / `advance_phase` executado. RESEARCH_LOG (P0047 cont.), Test Map (linhas UC-9), GATES.md, FINDINGS_CURRENT.csv atualizados. Registrado no Versus (testing, id 8e8e091d-5af2-46ad-ac71-401c3f3aedce). | P0047 |
| D0094 | 2026-09-06T03:44:58Z | **FECHAMENTO DA FASE 6 (Testes) com ressalvas documentadas** — GO explícito do VA-human (P0048). **UC-10 Lembretes = ACEITO COM WAIVER EXPLÍCITO** de validação manual da entrega local (não houve validação manual da entrega efetiva da notificação local em runtime; aceito com base em VAL-34–38, cobertura automatizada da lógica, paridade Tier-2 com o Android, ausência de evidência de divergência do cliente) — **não** registrado como PASS manual. **`edge_cases` = racional aceita** como suficiente para `met` (cobertura adversarial real da Fase 6 com achados reais + correções + testes de regressão). **Ações no Versus:** `mark_exit_criterion(6, manual_testing=met)` e `mark_exit_criterion(6, edge_cases=met)` → success; 3/3 critérios da Fase 6 `met` (`tests_passing` já estava). `check_all_safeguards()` → 0 violações. **`advance_phase(7)` NÃO executado** — VA-human exigiu confirmação final explícita (S2). **Matriz final:** PASS manual 10/12 (UC-1/2/4/5/6/7/8/9/11/12; UC-9 = feed/listagem/polling/badge/marcar + recebimento no app); UC-3 = paridade + EXT-DEP-02; UC-9 push remoto do sistema iOS = implementação concluída / validação runtime pendente por EXT-DEP-03; UC-10 = waiver. **Pendências carregadas para a Fase 7:** EXT-DEP-02 (UC-3, backend/Puppeteer), EXT-DEP-03 (UC-9 push remoto, Apple Developer Team / provisioning APNs institucional), waiver UC-10. EXT-DEP-01 (arquivo plist) resolvido 2026-09-05. Verificação: 96 testes verdes engine-witnessed; Package + App target BUILD SUCCEEDED. | Aceita — fechamento com ressalvas (decisão do VA-human) | VA-human (GO + waiver UC-10 + aceitação da racional de edge_cases) + GA (execução dos `mark_exit_criterion` e registro) | PROMPT_LOG/P0048.md, RESEARCH_LOG.md (P0048), GATES.md, FINDINGS_CURRENT.csv, Test Map atualizados. Registrado no Versus (testing, id da81f121-e318-41db-845a-fb2312b28e13). `advance_phase(7)` retido até "go" final do VA-human. | P0048 |
| D0095 | 2026-09-06T03:56:46Z | **FASE 7 passo 2 — Avaliação do produto (VA-human, via `AskUserQuestion`).** Veredito: o app iOS **atende aos requisitos do P0** (produto completo — login, cursos, inscrição, certificados, certidão, certificado externo, AVA/Moodle, notícias, notificações, lembretes) **com as 3 ressalvas externas já documentadas**. 10/12 UCs PASS manual + UC-3 em paridade; nenhuma lacuna restante é defeito do código iOS. Ressalvas confirmadas: `EXT-DEP-02` (backend/Puppeteer), `EXT-DEP-03` (Apple Developer Team / provisioning APNs institucional), waiver `UC-10` (entrega da notificação local não validada manualmente). **Backlog v2.0** (todos os 4 marcados pelo VA-human): (1) `EXT-DEP-03` validar push real com conta/Team Apple institucional do TJAC; (2) `UC-10` fechar o waiver (validar lembrete local em runtime; cadência/cap em uso real); (3) `EXT-DEP-02` acompanhar o backend de inscrição, revalidar UC-3 quando corrigido; (4) risco residual — `@State` semeado uma vez nas outras 8 telas com ViewModel inline (`CertificatesScreen`/`CertidaoScreen`/`CertExtScreen`/`MoodleWebScreen`/`NoticiasScreen`/`MateriaScreen`/`NotificationsScreen`/`HomeScreen`). | Aceita — avaliação do produto (decisão do VA-human) | VA-human (avaliação + seleção do backlog) + GA (registro) | `specs/validation/criterios-de-validacao.md` (seção "Fase 7 — Avaliação do produto"), `specs/references/lessons.md` (seção "Backlog para o ciclo v2.0"), `specs/technical/architecture.md` (seção "Fase 7 — decisões finais"), `specs/references/rastreabilidade-baseline.md` (arquivos inspecionados na implementação), RESEARCH_LOG.md (P0048 cont.) atualizados. Registrado no Versus (requirement, id 70459585-f5df-42fd-996b-2601390d7d19). | P0048 |
| D0096 | 2026-09-06T04:12:00Z | **FASE 7 passo 4 / ENCERRAMENTO DA METODOLOGIA.** Via `AskUserQuestion`, o VA-human escolheu "Projeto completo — encerrar a metodologia" (não iniciar ciclo v2.0; `start_new_cycle` **não** chamado). Ciclo v1.0 do IACDM completo — Fases 0→7: F0 score 96/100; F1–F4 V(2) 23 módulos + premissas A-1..A-11; F5 implementação Swift/SwiftUI iOS 17+; F6 96 testes automatizados 0 falhas engine-witnessed + 10/12 UCs PASS manual + UC-3 paridade, 3/3 critérios `met` com ressalvas (D0094); F7 specs/ atualizadas + produto atende ao P0 com 3 ressalvas externas (D0095) + 7 lições + backlog v2.0. Estado final: produto atende ao P0 (completo); ressalvas externas preservadas (não defeitos do código iOS): EXT-DEP-02, EXT-DEP-03, waiver UC-10; EXT-DEP-01 resolvido 2026-09-05. Base preservada para retomada: `specs/` (incl. `lessons.md`), `research/` completo, estado do Versus. | Aceita — encerramento (decisão do VA-human) | VA-human (escolha de encerrar) + GA (registro) | Nenhuma alteração de código. RESEARCH_LOG.md (P0048 cont.), GATES.md atualizados. Registrado no Versus (process, id b856d270-1e38-4e0b-93cb-9c9e1d944ce2). Metodologia IACDM concluída para o estudo de caso ESJUD iOS v1.0. | P0048 |
