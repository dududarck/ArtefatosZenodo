# P0038

- **Timestamp (recebimento):** 2026-09-04T20:04:53Z
- **Fase Versus:** Fase 6 (Tests), Iteração 1
- **Contexto:** captura PARCIAL do log pedido em P0037 — mostrou
  `/api/cursos` cancelado (`NSURLErrorDomain code=-999`), não
  `/[endpoint-formulario-inscricao]`.

## Prompt literal do VA-human

```
Captura parcial da reprodução:

O log mostrou:

[request] iniciando host=api-esjud.tjac.jus.br path=/api/cursos ...
[lifecycle] taskIdentifier=3 ... didCreateTask
[metrics] ...
transport FALHOU ... NSURLErrorDomain code=-999

Isso confirma uma task cancelada, mas é /api/cursos, não
/[endpoint-formulario-inscricao].

Ainda não aplique correção.

Quero que você avalie esta evidência e me diga exatamente quais linhas
devo capturar na próxima tentativa para isolar o request de:
/[endpoint-formulario-inscricao]

Priorize: EnrollmentViewModel instanceID; loadForm_started;
fetchForm_started; request de /[endpoint-formulario-inscricao]; didCreateTask;
watchdog; metrics; transport result; fetchForm_completed;
loadForm_finished.

Se /[endpoint-formulario-inscricao] também terminar com NSURLErrorDomain -999,
correlacione o cancelamento com o instanceID do EnrollmentViewModel e
verifique se a hipótese de recriação do ViewModel fica confirmada.

Não altere código ainda.
```

## Interpretação

Evidência parcial real recebida: `code=-999` (`NSURLErrorCancelled`) em
`/api/cursos`, não no request relevante. Não prova nem refuta a hipótese
de UC-3 ainda — mostra que HÁ cancelamento de tasks em algum lugar do
app (consistente com, mas não exclusivo de, a hipótese de recriação do
`EnrollmentViewModel`). VA-human pede um roteiro de captura preciso
para isolar `/[endpoint-formulario-inscricao]` na próxima tentativa, e a
correlação por `instanceID`/`taskIdentifier` caso o mesmo `-999`
apareça lá. Nenhuma alteração de código nesta entrada — só orientação
de captura + avaliação da evidência parcial.
