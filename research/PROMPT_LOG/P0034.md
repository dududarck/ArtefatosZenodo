# P0034

- **Timestamp (recebimento):** 2026-08-31T19:27:14Z
- **Fase Versus:** Fase 6 (Tests), Iteração 1
- **Contexto:** reteste manual do VA-human do UC-3 após a correção de P0033 — **ainda FAIL** ("Erro ao carregar formulário").

## Prompt literal do VA-human

```
Reteste manual do UC-3 após a correção: FAIL.

Comportamento real:
- o fluxo continua exibindo "Erro ao carregar formulário".

Não marque UC-2/UC-3 como PASS.
Mantenha a Fase 6 aberta.

Agora faça diagnóstico da execução REAL atual, sem presumir que seja novamente CPF vazio.

Instrumente e reporte exatamente a chamada feita ao tocar em "Carregar formulário":

1. CPF efetivamente enviado:
   - apenas quantidade de dígitos / validade;
   - NÃO logar o CPF real.
2. urlCurso efetivamente enviado:
   - path/query sanitizados;
   - compare com o valor retornado por /api/cursos.
3. request real:
   - método; host/path; status HTTP; tamanho do corpo; auth strategy.
4. se HTTP != 2xx:
   - registre a `message` retornada pelo backend; registre código/status; não exponha dados pessoais.
5. se HTTP 2xx:
   - registre tipo Decodable esperado; erro completo de decoding, se houver; confirme qual campo do payload diverge do modelo.
6. confirme se:
   - loadForm() usa o CPF digitado e não uma cópia antiga/vazia;
   - persistCPF() não ocorre antes de substituir corretamente o estado;
   - EnrollmentScreen está chamando loadForm(cpf:) com o valor atual;
   - não existe outra instância de EnrollmentViewModel sendo recriada com cpf vazio.
7. capture o erro real exibido internamente antes dele ser convertido na mensagem genérica "Erro ao carregar formulário".
8. compare a mesma requisição, com os mesmos parâmetros sanitizados, via curl contra produção.

Não aplique nova correção ainda.

Pare e apresente:
- request real;
- resposta real;
- causa objetiva;
- correção mínima proposta.
```

## Interpretação

Segundo achado adversarial sobre o mesmo fluxo (UC-3). A correção de
P0033 (captura/validação/persistência de CPF) não resolveu — o fluxo
ainda cai em "Erro ao carregar formulário". Instrução: diagnóstico da
execução real, instrumentando a chamada exata; NÃO presumir CPF vazio;
comparar `urlCurso` enviado vs `/api/cursos`; reproduzir por curl com
parâmetros sanitizados; capturar o erro interno real antes da conversão
para a mensagem genérica. Nenhuma correção nova ainda — parar e
apresentar request real, resposta real, causa objetiva e correção
mínima.
