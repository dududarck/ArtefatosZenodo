# P0019

**Timestamp:** 2026-08-31T03:58:44Z

**Prompt (verbatim):**

> Resultado real do novo smoke test P0 após D0053:
>
> - Login Moodle: PASS
> - Nome na Home: FAIL
> - Inscrição em Curso: FAIL
> - Cursos/Home: FAIL
> - Demais fluxos: não testados nesta rodada porque a falha de rede bloqueia as funcionalidades dependentes da API.
>
> Evidência de runtime:
>
> `nw_endpoint_flow_failed_with_error`
> `api-esjud.tjac.jus.br:443 failed`
> `Socket is not connected`
>
> A correção anterior de URLSessionTaskDelegate NÃO resolveu empiricamente o problema no app.
>
> Mantenha a Fase 5 aberta e registre este segundo smoke test como FAIL real.
>
> Agora diagnostique a divergência entre:
>
> - curl com o mesmo .p12 funcionando com HTTP 200;
> - URLSession do app falhando antes de estabelecer conexão útil.
>
> Faça instrumentação objetiva do fluxo mTLS no app antes de nova correção:
>
> 1. registre quando o URLSessionDelegate recebe challenge;
> 2. registre quando o URLSessionTaskDelegate recebe challenge;
> 3. registre o `protectionSpace.authenticationMethod`;
> 4. registre host e porta;
> 5. confirme se o challenge `NSURLAuthenticationMethodClientCertificate` é realmente recebido;
> 6. confirme se `SecPKCS12Import` retorna sucesso;
> 7. confirme se a identity é extraída;
> 8. confirme se os certificados da chain são recuperados;
> 9. confirme se `URLCredential(identity:certificates:persistence:)` é criado;
> 10. confirme se o completionHandler é chamado com `.useCredential`;
> 11. registre o erro final completo de `URLSession`, incluindo NSError domain/code/userInfo;
> 12. compare esse fluxo com uma requisição mínima isolada usando o mesmo Networking module e o mesmo endpoint;
> 13. não altere TLS, não use trust-all e não altere backend.
>
> Não faça nova correção por hipótese antes de identificar em qual etapa o fluxo mTLS está quebrando.
>
> Os erros de haptics e Auto Layout devem ser registrados como ruído secundário, não como causa da falha de API.
>
> Pare e apresente o diagnóstico antes de qualquer mudança estrutural.
