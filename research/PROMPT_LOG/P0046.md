# P0046

- **Timestamp (recebimento):** 2026-09-06T02:11:14Z (hora do sistema)
- **Fase IACDM/Versus no momento:** Fase 6 (Testes), iteração 1
- **Origem:** VA-human (chat)
- **Safeguards no momento:** S0–S5 ok, S7 ok, S6 warning (0/3 — aviso padrão de implementação ativa)

## Prompt preservado (verbatim)

> Atualização de estado — UC-9 / Push FCM.
>
> A integração do Firebase/FCM no cliente iOS está implementada e validada até o limite possível no ambiente atual.
>
> Novo bloqueio confirmado:
> - não há Development Team institucional configurada no Xcode;
> - o app exige a Team Apple Developer do TJAC para assinatura/provisionamento;
> - sem essa Team, `aps-environment` não entra efetivamente no provisioning;
> - portanto o registro APNs e a validação de push real em dispositivo físico não podem ser concluídos agora.
>
> Classificação:
> - NÃO tratar como defeito do código iOS;
> - registrar como pendência técnica/externa de validação do ecossistema institucional;
> - preservar que `GoogleService-Info.plist` já foi recebido e integrado;
> - preservar que Firebase init, Messaging, registro de token, backend, handlers e testes já foram implementados;
> - o que falta é somente validação runtime real com assinatura institucional/APNs.
>
> Atualize: RESEARCH_LOG; DECISIONS; Test Map; FINDINGS_CURRENT.csv; GATES, se aplicável.
>
> Sugestão de status: UC-9 = IMPLEMENTAÇÃO CONCLUÍDA / VALIDAÇÃO REAL PENDENTE POR DEPENDÊNCIA INSTITUCIONAL
>
> Registre a dependência como: EXT-DEP-03 — Apple Developer Team / provisioning APNs institucional — ou atualize EXT-DEP-01, se a taxonomia atual preferir manter toda a cadeia Firebase/APNs no mesmo item.
>
> Não marque UC-9 como PASS manual. Não trate como FAIL do cliente. Não bloqueie a continuação da passada exploratória da Fase 6 por causa dessa dependência externa.
>
> Agora prossiga para o BLOCO D — UC-10 Lembretes + edge cases.

## Ação atribuída

1. Registrar o bloqueio confirmado (ausência de Apple Developer Team institucional do TJAC no Xcode → `aps-environment` não entra no provisioning → registro APNs e validação de push real em dispositivo indisponíveis) como dependência externa **EXT-DEP-03**, referenciando EXT-DEP-01 (cadeia Firebase/APNs).
2. Classificar **UC-9** como *IMPLEMENTAÇÃO CONCLUÍDA / VALIDAÇÃO REAL PENDENTE POR DEPENDÊNCIA INSTITUCIONAL* — não PASS, não FAIL do cliente.
3. Atualizar RESEARCH_LOG, DECISIONS (D0090), Test Map, FINDINGS_CURRENT.csv, GATES.
4. Prosseguir para o **BLOCO D** — UC-10 Lembretes de curso + edge cases: apresentar roteiro manual curto e objetivo (GA apresenta; VA-human executa; registra PASS/FAIL; em FAIL diagnostica/corrige/reteste antes de seguir; `manual_testing`/`edge_cases` não marcados `met` até tudo concluído).

Sem alteração de código nesta entrada (item 1–3). Fase 6 aberta; sem avanço para a Fase 7.
